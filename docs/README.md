# Docs

**This folder holds specs with open work, and is the single tracker for what is built,
what is in flight, and what is left.** Anything shipped and closed moves to `archive/`.

There is deliberately no second status file. This repo carries a scar from three
hand-maintained parallel lists in `shared/intel/registry.mjs` that disagreed until all
three were derived from one table — the same failure applies to progress tracking. Update
this table in the same commit as the work.

Last updated 2026-08-28.

---

## Test suite classification (live-save migration)

`npm test` runs `scripts/run_unit_tests.js`, which walks `tests/` recursively and
**excludes** `tests/live/`. `npm run test:live` runs only the live integration tests.
Committed intel fixtures live in `tests/fixtures/snapshot-*-intel.json` and are loaded
via `tests/fixtures/frozenSnapshots.js`. `tests/noLiveSaveInUnitSuite.test.js` fails the
unit suite if any file outside `tests/live/` calls `loadFilteredSnapshot`, bare
`loadSnapshot()`, or `latest: true`.

| File | Bucket | Data source |
| :--- | :---: | :--- |
| `tests/controlPointCap.test.js` | A | `loadFixtureFilteredSnapshot` / `queryFixtureIntel` |
| `tests/mineModuleOutput.test.js` | A | fixture intel snapshots |
| `tests/miningTechBonus.test.js` | A | fixture intel snapshots |
| `tests/refitAdvisor.test.js` | A | fixture intel snapshots |
| `tests/spaceMiningBonus.test.js` | A | fixture intel snapshots |
| `tests/miningExpansion.test.js` | A | fixture intel snapshots |
| `tests/markdownExports.test.js` | A | byte fixtures + synthetic snapshot; HTTP smoke uses Express (fixture presence gate) |
| `tests/researchRanking.test.js` | A | fixture intel snapshots |
| `tests/researchCategoryBonus.test.js` | A | fixture + synthetic saves |
| `tests/techGraph.test.js` | A | fixture + synthetic graph |
| `tests/driveExplorer.test.js` | A | fixture + synthetic turn-1 |
| `tests/drivePathModal.test.js` | A | fixture + synthetic turn-1 |
| `tests/fleetEngagement.test.js` | A | fixture + synthetic turn-1 |
| `tests/holdGround.test.js` | B | synthetic only (live block moved) |
| `tests/riskTolerance.test.js` | B | synthetic + engine fixtures (live block moved) |
| `tests/snapshotLoader.test.js` | B | resolveObserverId + parse_save only |
| `tests/noLiveSaveInUnitSuite.test.js` | guard | scans unit files for forbidden live-save patterns |
| `tests/live/holdGround.live.test.js` | C | `loadFilteredSnapshot({ latest: true })` |
| `tests/live/riskTolerance.live.test.js` | C | `loadFilteredSnapshot({ latest: true })` |
| `tests/live/snapshotLoader.live.test.js` | C | `loadSnapshot()` + `loadFilteredSnapshot` |

---

## Open work

**Empty as of 2026-08-24.** Every tracked item is in the Shipped table below or in
*Closed as needing no work*, each with its measurement. Of the entries that remain
under follow-ups, two are worktree environment quirks rather than defects in the
dashboard.

The next thing this repo owes its owner is a **publish and deploy** — the hosted site is
serving snapshots that predate roughly 114 commits of correctness work, including several
figures that changed materially (the total-war gate, research costs, the directive engine's
primary recommendation). `npm run push:supabase` then a worker deploy; both are the owner's
action, and `push:dry-run` exists to rehearse it.

### Small follow-ups, unassigned

- **24 `<link>` tags is 24 requests.** Multiplexed on HTTP/2 by the hosted worker and
  individually cacheable, so this is a note rather than a problem — but if it ever measures,
  the answer is a build-time concatenation in `npm run build:site`, not an `@import` chain,
  which would serialise the round trips instead of removing them.
- **A worktree has no `config.json`.** It is gitignored, so `npm run test:live` skips when
  no save is configured. The default `npm test` unit suite no longer reads the live save
  folder — only `tests/live/` does.
- **A worktree has no `dist/` either, so the suite reports 2 skips rather than 1.**
  `tests/directiveEngineV2.test.js` skips the published-snapshot degrade path when
  `dist/data/snapshot-player-4712.json` is absent, and that file is build output from
  `npm run build:site`. It costs one *passing* test, so an agent reconciling counts against
  a main-checkout baseline will find themselves one short with nothing failing. Copying the
  file across from the main checkout restores it (verified 2026-08-22: 1329 / 1328 / 0 fail
  / 1 skip with it, 1329 / 1327 / 0 fail / 2 skip without).
- ~~**`BENCHED_RENDER_LIMIT = 5` in `public/v2/js/components/directive-board.js` still slices
  in generation order.**~~ **CLOSED 2026-08-22** by the bench-diversity change — see the
  Shipped row "bench diversity (tracker item 6, second pass)". The constant is gone, the
  board renders every row the engine hands it, and the comparator moved to
  `shared/benchSelection.mjs` rather than being duplicated in browser ESM.
- ~~**The strategic commentary's throughput figure conflates queued SHIPS with shipyards.**~~
  **CLOSED 2026-08-22 — measured, and the dividend turned out to be wrong too.** See the Shipped
  table row "the shipyard throughput model".
## In flight

**[`cross-aware-advisor-spec.md`](cross-aware-advisor-spec.md)** — design, not built.
The directive engine is councilor-facing and cannot see an incoming alien fleet:
, ,  and  appear in ZERO files under
. The spec covers the join (shipyards carry ,
arrivals carry none), what is measured vs inferred in the hate reasoning, and the
six-step phasing. Steps 1-3 are worth doing regardless of the advisor.

**[`ship-designer-spec.md`](ship-designer-spec.md)** — design, not built. Requested
2026-08-29: build *new* designs in the dashboard and read their cost and performance,
with omniscient offering every part and player mode only what this faction has
researched. **Four of the five hard parts are already solved** — drive↔reactor
compatibility is a direct `requiredPowerPlant` ↔ `powerPlantClass` join, propulsion
and build time are existing calibrated engines, and the research gate is already in
the snapshot as `unlockIndex` (436 gates, 1,223 entries, **0 unresolved**).

**Heat is the real work.** `Assembly-CSharp.dll` names the model —
`get_wasteHeat_GW`, `get_radiatorMass_tons`, `get_radiatorsBuildCost`,
`DoesDriveHeatExceedRadiatorAndOverheatInOneSecond` — so radiator mass is **derived
from heat, not chosen**, and the constraint is per-second. The formula itself is
**not verified**: the spec carries a labelled hypothesis with plausible magnitudes,
and two blockers. `drive.cooling` has a third value, `Calc`, on **186 of 541**
drives whose meaning is unknown; and `ilspycmd` needs .NET 10 while this machine has
9.0.18, so the decompile route that established `shipBuildTime.mjs` is closed for now.

**View layout onto MUI `Grid2`** — **DONE**, all six views, `.init-view__grid` deleted.
This is the payoff of the React migration: the widgets were already components, the
layout was not. Each view moves from the hand-rolled `.init-view__grid` to
`src/v2/components/TwoColumnGrid.jsx`, one view per commit, each proving it changed
nothing.

| view | commit | proof |
| :-- | :-- | :-- |
| THREAT | `3d0d9f5` | geometry unchanged |
| COMMAND | `39bbc69` | nine panels, twelve differences, none of them text |
| EXPANSION | `32f8de4` | 16 differences, all one property that stopped applying |
| FLEET | `862d620` | 145 values, zero differences |
| DRIVES | `8026533` | 121 values, zero differences |
| RECORDS | `19212bb` | 913 values, zero differences — **and it caught the 900–1192px band** |
| *delete the CSS* | `5107c93` | 5,130 values, six views, zero differences |

`23c2374` put `TwoColumnGrid` on real `Grid2` first. **Both follow-ups are done**,
and the second one is the reason this entry is worth reading:

- **`.init-view__grid` is deleted** (`5107c93`), along with its selectors in
  `15-responsive.css`. `.init-grid-layout`, `.init-view__span` and
  `.init-view__full-card` all survive deliberately — four React panels still emit
  `__span`, and its `min-width: 0` is live even though its `grid-column` is inert
  under flexbox.
- **A width band that five conversions shipped without ever measuring.** Legacy is
  `repeat(auto-fit, minmax(min(100%, 560px), 1fr))`, so it takes a second column
  only at 2×560 + 24 + 48 = **1192px**. `TwoColumnGridItem` asked for `md`, and MUI's
  `md` is **900px** — `theme.js` overrides no breakpoints. Between 900 and 1192 the
  two disagreed, and the captures ran at 375/414/768/desktop, which steps over that
  band without landing in it.

  **RECORDS caught it, because it bites hardest there**: the 560px floor was measured
  from RECORDS' own cards, and at 1000px the ledger went **918px → 430px and gained a
  horizontal-swipe affordance it never had**. Fixed in `19212bb` by `md: 6` → `lg: 6`
  (MUI's 1200px, within 8px of legacy). **It was never a RECORDS defect** — THREAT
  and COMMAND carried it too, invisibly.

  The standing lesson: **1000 and 1100 are now first-class capture widths**, and
  `scripts/capture_*_view_proof.js` covers all six views so the next layout change
  re-proves itself in one command.

---

## React migration status

**COMPLETE — see [`react-migration-progress.md`](react-migration-progress.md).**
All 16 components are React under `src/v2/panels/`; `public/v2/js/components/` is
empty. Nine defects were fixed at the port rather than separately. That doc
carries the definition of done, the verification procedure, the four issues left
behind, and measured lane findings.

---

## Shipped

| doc | commit | note |
| :-- | :-- | :-- |
| **Third slice of #21: Fleet Engagement onto `<Value>` and onto MUI in one pass** | — | **The first slice to fold the MUI move in with the `<Value>` conversion**, per [`frontend-architecture.md`](frontend-architecture.md) — opening a panel twice and proving "nothing changed" twice costs more than doing both at once. `src/v2/panels/FleetEngagement.jsx` no longer owns a private em-dash constant or the four private formatters (`int`, `count`, `txt`, `plural`) that each re-decided what an absent value looks like; presence now resolves through `<Value>` / `resolveValue()`, and the panel's layout hosts are MUI `Box` / `Stack` with spacing read from `theme.initiative.space`. The table moved onto `DataTable`'s `rows` prop rather than hand-rolled `<tr>`/`<td>`, keeping the custom `<tbody>` only for the empty-state row. **THE BAR WAS ZERO RENDERED CHANGE, AND IT WAS MEASURED, NOT ASSERTED:** 18 payload scenarios (both fixture modes, the four unavailable branches, beyond-ΔV, band, floor, unrateable, co-located, threat row, reach-unknown, omitted null/zero, and an everything-stripped sweep) captured in a real Chromium before and after and diffed across `innerText`, `textContent`, character count, every `td`/`th` text, every `title`, every row class, the computed font/colour/border/background/display of 24 selectors, and the bounding rects of every cell, summary item and block — **zero differences in every field.** Node count rises (310 → 340 on the live fixture, from the `<Value>` spans inside composed sentences) and `data-value-state` goes **0 → 142**, which is the entire point: the affordance is now structural. **On the harness page, mount `#fleetEngagement`: player 2,259 chars / 340 nodes / 142 stamped, omniscient 2,258 / 340 / 142.** **The coverage hole that let the Executive Boards slice ship unverified was found again here:** neither `tests/fleet-engagement.test.js` nor `tests/fleetEngagement.test.js` contained a single `data-value-state` assertion. **Four tests added, each proven to fail against its own deliberate mutation before being trusted** — hoisting presence to the row (the cascade defect), hand-writing one dash back, rendering `p80` in place of `bandLabel` (register #13), and captioning `beyond-modelled-range` as `UNWINNABLE` (register #14). **`bandLabel` is rendered byte-for-byte and never recomputed**, so p20–p80 survives and a partly rateable fleet still reads `at least 7–8 hulls`. **One thing the stylesheet forced:** `.fe-cell span { display: block }` would have stacked any figure composed into a sentence onto its own line, so those nodes take their display back through MUI (`sx={{ '&&': { display: 'inline' } }}`, (0,2,0) against (0,1,1), no `!important`, no new CSS); the two `title` attributes stay string-only through `resolveValue().text`, because an attribute cannot host an element. Type scale, registers and table styling stayed on the stylesheet deliberately: MUI's `Typography` variants set `fontSize` and `lineHeight` at a specificity that beats a single-class `.fe-*` rule, and restating them was not worth the drift against a zero-change bar. |
| **`docs/engagement-matchup-spec.md`: the abandoned currency leaves the exports, and one caveat is corrected** | — | **Three changes, all 2026-08-28.** **(1) `/latest-threats.md` was still printing the model the spec abandons** — `Hulls needed: 54–58 hulls` on every row, `Observer can field: INSUFFICIENT — 38 reachable hull(s) vs 58 needed`, and `best design Cimarron rated 19,783` in the preamble. `## Per-Fleet Engagement Estimates — MODELLED, NOT MEASURED` is now `## Per-Fleet Battle Composition — READINGS, NOT A COMBAT-VALUE SCORE`, composed by **calling** `composeBattleSide` / `saturationVerdict` from `shared/battleComposition.mjs` — the same functions §1d calls, so the two documents cannot disagree about a shot count. **What was kept, and why:** fleet identity, `At:` (position, destination, ETA, dominant weapon), `Reach:` and the ranking are read from the fleet records and the ΔV table, not from any rating, so `buildFleetEngagement` stays the source of all four; only its `requirement`, `fieldable` and `ownForce.rating` are no longer read. `ENGAGEMENT_VERDICTS` was dropped from the import so re-importing it is a visible step back. **Each row now carries** the fleet's own composed mounts/shots/PD-immune/armour/join, and the saturation verdict against the observer's ships **at that fleet's engagement point** — not the whole board, which §1d already gives. **Three absences are told apart** rather than collapsed: no weapon catalogue, no body an engagement can be located at, and no observer ship at that body (`NO SCREEN THERE`, stated as an absence of *your* hulls — `composeBattleSide([])` would otherwise render it identically to a fleet with zero point defence). **Measured on the committed fixtures:** player 5,929 → 8,700 B, omniscient 5,924 → 8,720 B, against a 10,240 cap; nothing was shed by the ladder. **(2) War-room §11's hull-threshold table came out**, with the `Own best combatant … combat rating 19,783` line it was denominated in and the calibration paragraph that qualified it — the spec's "what would make this wrong" list names keeping a hull count beside a composition reading as a defect in its own right. What survives is what is not combat-value derived: the beats (delta-V / armour / hull-**count** axes), the hate-vent horizon, and the production pipeline and throughput. The `advice` line is withheld **only when it is itself a hull count** — one of `grammar.js`'s three branches embeds the sweep's own `bandLabel` — decided by testing the advice against the tiers, not by a regex on digits. A sweep that genuinely failed still prints its own reason, because "we declined to print it" and "it did not run" are different facts. **The war room was budget-bound at 30,209 / 30,260 B against a 30,720 cap, so the freed bytes did not shrink the document — they bought back measured detail:** §3 now suppresses the weapon/armour/propulsion line on **13 of 21** hostile contacts in player (was 15) and **11 of 21** in omniscient (was 12). **(3) The interception caveat was wrong in the reassuring direction.** It said *"if a mount intercepts more than one shot, every shortfall this verdict reports is understated"* — a hedge that existed only because the ratio was a guess. The owner confirmed on **2026-08-28** that one PD mount can nullify roughly one weapon, so `INTERCEPTION_ASSUMPTION` now carries `source` / `stated` / `measured: false` / `inTemplates: false`, `verified` stays `false` (it means *read from the game*, and the interception rules never were), and `consequence` names what the ratio does **not** model — quality, range, missile acceleration — instead of a direction of error nobody can now sign. The **2× overwhelm multiple keeps its own earlier date and its rule-of-thumb label** ("probably", "a safe bet"); §1d and the threats preamble print the two as separately attributed claims rather than collapsing both into "assumption". **Six tests were rewritten and one deleted, each named in the commit body.** |
| **`docs/engagement-matchup-spec.md` phase 1 reaches the AI exports (`/latest-war-room.md` §1d)** | — | **The battle composition existed only in the browser's BATTLE tab, so every LLM reading the exports still had only the combat-value model the spec abandons.** New section 1d, sitting under 1c because 1b says what is moving, 1c says what to do, and 1d says whether the force can fight. It carries both sides' point-defence mounts, PD-targetable shots (missiles **and** kinetics), PD-immune weapons, median armour and the weapon join rate; both saturation verdicts; the per-body rows where both sides have ships present. **Measured on the committed fixtures, identically in player and omniscient:** ours 38 ships / 27 PD mounts / 280 targetable shots / 21 PD-immune; theirs 534 / 381 / 3,331 / 661. Their salvo saturates a 54-shot screen with 3,277 to spare; ours falls 482 short of theirs. **Four properties are pinned by test, each a way it could have lied:** PD-immune weapons are their own figure and the verdict's shot count is cross-checked against the composition line, so folding them in fails (verified by deliberately folding them — the first version of that assertion was tautological and passed the break); kinetics are counted with missiles (`targetable == missile + targetable kinetic`); the interception assumption and the 2× rule print with the user's attribution, the date and the direction of the error; and a snapshot with no `componentStats` says **Composition NOT READ** rather than rendering a zero-mount screen. **No hand-in from the serving runtime** — unlike §1c, §10 and §11, both inputs (`componentStats`, `weaponLoadout`) travel on the filtered snapshot and on every published Supabase row, so the Cloudflare worker composes the same numbers. `weaponTemplatesFromComponentStats` and `WEAPON_FAMILIES` moved from `src/v2/panels/battleSuggestionUtils.mjs` into `shared/battleComposition.mjs`; the panel re-exports the same function objects. **The byte trade, stated rather than left to the ladder:** the war room was 30,314 / 30,207 bytes on the committed fixtures against a 30,720 cap — 406 and 513 bytes of headroom — so §1d's 2,466 bytes had to evict something. **§9 (drive explorer) paid.** It was a `fixedBlock` with no ladder position, so the only thing that could shed a refit what-if for ONE design was `clampOrder`, which the ladder never reaches — it printed in full while the ladder had already emptied every research entry in §8 and stripped the weapon/armour/ΔV line from 11 of 21 hostile contacts in §3 (9 of 21 in omniscient). It is a one-entry `listBlock` now, dropped fourth in the ladder, and its own `clampOrder` comment had already ranked it "the last thing anyone needs in a war-room brief cut to the bone". Net on the fixtures: 30,209 / 30,260, §9's 1,924-byte body replaced by a 210-byte pointer to `/api/intel/drive-explorer`, and §3 shed 15 of 21 weapon/armour lines in player (was 11) and 12 in omniscient (was 9) — four and three more contacts lose a per-fleet category tally that `/api/intel/fleets` still carries in full, in exchange for that same data resolved against the game's own `isPointDefenseTargetable` field for the entire board. **On the live save** (`Autosave.gz`, 9/2/2046, measured against the pre-change renderer read out of `HEAD`) the trade is cheaper still: player 30,494 → 30,343 — a net *saving* of 151 bytes with §3 untouched — and omniscient 30,301 → 30,328. That board is where the section earns it: 248 observer PD mounts against 4,887 incoming targetable shots, and 778 hostile PD-immune weapons no screen can touch. **A gap worth naming:** §1d lists only bodies where BOTH sides have hulls, so the live board's most threatened body — Ganymede, 28 hostile hulls present and 45 arriving in 0 days — has no row, because the observer holds nothing there. The heading says so on every render, not only when the list is empty: an absence here is an absence of *your* ships, not of theirs. **`world.military.theaterForce` was NOT rendered, deliberately.** Measured against both fixtures, every content-bearing field on it is either the abandoned currency (`own.rating` / `opponent.rating`, both `_unnormalizedCombatValue`-derived) or the hull count `d0a671d` removed and the spec forbids reintroducing "in any form" (`opponentFleets[].requirement`, e.g. "54–58 hulls" at Earth in player against "39–41" in omniscient, and Titan's Victor-37 flipping from 4–5 to 5–6 the other way). `own.rating` is null at 11 of 12 bodies in both modes, so the per-body comparison is unformable at every body a hostile force actually occupies, and the whole structure serialises to 45–58 KB — 1.5–1.9× the entire document budget. §1c's engine-supplied note already tells a reader where it lives and why no comparison is made; §1d adds one line naming §11's hull counts and saying which of the two rests on readings, which is what the spec's "two numbers where one is known wrong" hazard demands. |
| **the theater-defence browser panel (THREAT view, `#theaterDefence`)** | — | **Closes the gap the row below names: `theaterDefence` was on `/api/v2/briefing` and in war-room §1c, and no v2 view rendered it for a human.** `src/v2/panels/TheaterDefencePanel.jsx` renders it, with the decisions in `theaterDefencePanelUtils.mjs` so they can be asserted without a DOM. **It reads the briefing the controller already holds** — `renderDashboard()` hands it `{ engineDirectives: state.briefing?.engineDirectives }` — rather than fetching `/api/v2/briefing` a second time, because that route runs the engine per call and two runs against one save have nothing guaranteeing they agree; that is the same reason `server/http/routes/snapshot.js` hands the war-room renderer the one briefing it already generated. So the panel publishes `render` and deliberately **no `fetch`**, unlike its neighbour. **Registered in all five places a rendered panel needs:** the `theater-defence` DataTable variant in `src/v2/components/tableVariants.js` (an unregistered variant throws BY DESIGN — that is how `HostileMovementPanel` shipped with `variant="default"`, rendering zero characters with a console TypeError while `npm test` stayed green), the mount in `public/v2/index.html`, the `threat` view's panel list in `mission-control.js` so `assertViewRegistryIntegrity()` fails loudly if a layout pass moves it out, the `.td-scroll-hint` / `.td-table-wrap` pair in `syncScrollHints`, and the four `main.jsx` sites. New stylesheet part `26-theater-defence.css` (the `codeIndex` guard's part count and last-part assertion moved 25 → 26 with it). **Four collapses the panel refuses**, each with its own `data-` attribute so a test asserts the claim rather than reading a glyph: `arrivalTimingKnown` **null** (nothing inbound) versus **false** (inbound, no date) versus **null beside an inbound or unreadable fleet count** (the flag itself was not read) render as three different `data-contact-state`s; `buildRace: null` on a quiet body is `not-applicable` while on a threatened body it is `not-run` with the refusals saying which; `shipsCompletingBeforeThreatArrival` is **omitted entirely** where nothing is inbound rather than printed as "0 completing before contact", which would report a race never run; and an empty findings list with a positive `findingsTotalCount` says the cap dropped them, never "no theater is at issue". **Refusals and citations are content, not chrome:** every refused check prints with its reason, and the citation trail follows §1c's shape — the shared basis printed once as a genuine intersection across rows (so it can never claim a reading some row lacks), each row carrying its own count plus its extras. Every figure goes through `<Value>`, and a browser test fails on any standalone `—` outside one (defect #21). **Measured on the live save, 13 Nov 2042, observer 4712: the mount renders 17,932 characters / 267 nodes, and player and omniscient are BYTE-IDENTICAL** — `.innerHTML` compared, not just counts. That board is 6 findings, all `HOLD`, five with nothing inbound and Callisto at 363 days with one build-race refusal. **The `BUILD` path was verified separately against real engine output**, not a hand-written fixture: the 20 Oct 2042 briefing captured earlier the same day renders Mercury `BUILD` / `BUILD LANDS FIRST` / Gunship at yard 315317 / 9 days against 24 / margin +15 / 12 citations, at 22,144 characters and 324 nodes, with Ganymede's "no shipyard here — a measured absence of build capacity" refusal beside it. **Both new tests were proven to fail before they were trusted:** reverting the variant to `"default"` turned 11 of 13 browser tests red (the two survivors are the unavailable-block cases, which never reach the table — exactly right), and collapsing `arrivalTimingKnown === false` into "nothing inbound" turned the utils test red. |
| **the theater-defence block reaches the AI exports (`/latest-war-room.md` §1c)** | — | **`engineResult.theaterDefence` (shipped in `b5eea01`) existed only on `/api/v2/briefing`, which under this repo's own rule means it was invisible to every LLM reading the `.md` files — the exact gap measured on 2026-08-21, when four of one session's six additions never reached the exports.** It now renders as **§1c**, immediately after §1b, because §1b says what is moving and §1c says what to do about it. **Three places, in the order they fail:** *(1)* `server/briefingGenerator.js`'s `engineDirectives` is an explicit ALLOW-LIST of named fields, not a spread, so `theaterDefence: engineResult.theaterDefence` had to be added there — verified before the change that `/api/v2/briefing` carried no such key, which is what omitting it looks like: silent, and indistinguishable downstream from a rendering bug. *(2)* `server/http/routes/snapshot.js` hands it to the renderer off the **same single briefing** already generated there for `cyclePlan`, `primary` and `strategicCommentary` — two calls would be two engine runs against one save with nothing guaranteeing they agree. *(3)* `shared/markdownExports.mjs` renders it, resolving `options.theaterDefence ?? snapshot.missionControlBriefing.engineDirectives.theaterDefence ?? <not read>`, so **the Cloudflare worker needs no change**: `theaterDefence` is a sibling of `cyclePlan` on the object the published rows already carry. **Measured on the live save (17 Sep 2042, observer 4712), identical in both modes**: state `INBOUND TO TRACKED THEATER`, 8 of 8 threatened theaters carried, 0 omitted by the block's own cap; **Mercury BUILD** — 1 inbound hostile fleet / 105 ships, nearest arrival 57 days (2042-11-13), 18 hostile fleets / 25 ships already present, ours 30 ships and 12 yards, **build race `build-lands-first`, fastest hull Gunship at yard 315317, 9 build-days against 57 days to contact, margin 48** — and seven `HOLD` rows with nothing inbound. §1c renders **byte-identical in player and omniscient apart from the `mode=` in its two endpoint pointers**. **Byte budget: 30,310 player and 30,204 omniscient, both under the 30,720 cap**, from 29,067 / 28,647 before — so the brief was already at **94.6% of its ceiling** and the 3,920-byte section cost **2,677 bytes (player) / 2,363 (omniscient) of announced degradation** through the existing ladder, not a raised cap: §8 lost all 3 global slots, 6 observer projects, 8 category bonuses and 2 chains (3,566 → 2,412), §3 lost the second detail line from 8 of 14 contacts, §2 lost its weapons/PD line, and §1c lost its own per-row citation lines. Every one of those says so in its own body with a count and a pointer. **A real bug fell out of writing the ladder entries:** `renderWithByteBudget` looked a block up by key and called `block.entries.filter(...)` on it, so a `reduce`/`drop` stage naming a key that resolves to a **fixed** block threw `TypeError: Cannot read properties of undefined` — which §1c is on both of its unavailable paths. Fourteen tests in `markdownBudget.test.js` went red on it; the guard skips non-list blocks, where `clampOrder` is the right mechanism anyway. **Four things the section refuses to collapse.** `threat.arrivalTimingKnown` is `null` and not `false` when nothing is inbound, so a quiet body reads **"nothing inbound"**, never "arrival time unknown" — different claims. The friendly line **omits its completion clause entirely** where nothing is inbound rather than printing "0 completing before contact", which would measure a race that was never run. Every count is `localeOr`, so an unread one is `UNAVAILABLE` and never 0. And an empty findings list with a positive `findingsTotalCount` says **all rows were omitted by the cap**, never "no theater is at issue". **The citation trail is printed, not asserted:** the 9 readings every row shares are rendered once as a section-level basis line — a genuine intersection across rows, so it can never claim a citation some row lacks — each row carries its own **count on the posture line, which never degrades**, and the 3 extras Mercury cites beyond the shared set (`engine/military` ×2, `shared/shipBuildTime` ×1) go on a sheddable line below it. **Refusals render as content:** a `CANNOT_ADVISE` row prints one line per refused check with its reason, and dropping those reasons is the LAST §1c compaction step, below every cheaper cut in the document. **Tests:** `MAX_THEATER_DEFENCE` (12 findings, both refusals populated, 13 citations each, all five engine notes) joins `HANDED_IN` so the 30 KB cap is now proven with a full §1c, §10 and §11 at every growth level; the header-survival test gained `1b` and `1c` and asserts §1c still prints its "not read" statement at a 3 KB budget; and `sectionText`'s terminator was `/^## \d+\./`, which did not match `## 1b.` — so `sectionText(rendered, 1)` had been silently returning sections 1, 1b and 1c joined, and an assertion aimed at the alien-threat block was reading three. **Not done, and out of scope by the brief: the browser panel.** §1c is in the exports and on `/api/v2/briefing`; no v2 view renders it. **Closed by the row above** — the THREAT view renders it now. |
| **`docs/react-migration-spec.md` (Round 3: the first two component migrations)** | `2c1427f`, `7a48add`, `a82c2b0` | **Two vanilla panels are gone and React renders them through the Track E primitives — and both ports fixed a registered defect rather than carrying it across.** `mc-budget` (187 lines) and `strategic-commentary` (202 lines) were deleted, not wrapped. **`mc-budget` closed defect #3:** the vanilla did `(num(difficultyMultiplier) \|\| 0)` where `num()` deliberately returns null, so an unmeasured multiplier rendered a confident floor; the port renders `PROJECTED FLOOR UNAVAILABLE`, asserted by a test that **did not exist before**. **`strategic-commentary` closed #13 and #14**, both in one ternary: `bandLabel` was rendered alone although `server/commentary/simulation.js:576` says in a comment *"The band never travels without what it covers"*, and `winnable: false` printed a red `UNWINNABLE` although `shared/engagementModel.mjs:58` says the flag means "above the ceiling I swept", **NEVER** "this cannot be won". The port renders `p20–p80 over N seeds`, a `bandCovers`/`bandExcludes` footnote, a partial-seed understatement warning when `winnableRatio < 1`, and **`more than 24 hulls`** — reading `uncertainty.maxHullsSwept` and falling back to `MAX_SIMULATED_HULLS` **imported from the shared model**, not a literal, so the panel and the sweep cannot disagree. It also uses strict `=== true` / `=== false` and renders `UNAVAILABLE` for anything else, where the vanilla's truthiness test would have called an unknown UNWINNABLE. **The inversion is the part worth reading:** `shared/markdownExports.mjs:2602-2631` already rendered all of this correctly, so for once the **AI export was right and the browser had lost the caveat** — the opposite of this repo's usual direction, and the reason the fix needed no new wording invented. **Verified:** `mc-budget` 12/12 and `strategic-commentary` 10/10 browser characterisation tests, every original assertion intact; `verify_mobile_overflow.js` PASS with **0 unreachable** at 375/414/768 in both modes and COMMAND at **2.94 screens** against a 3.25 budget; `verify_v2_navigation.js` **0 console errors, 0 network errors**; `reactCoexistence.test.js` green, so `assertViewRegistryIntegrity()` still passes with React mounted. **Not verified, and this is the honest gap: neither port has a computed-style diff.** That check is blocked by **defect #16** — `verify_computed_style_baseline.js --save` stamped a fingerprint for a save it never rendered, and after a partial fix (`a82c2b0`) the pin still reaches only 1 of 11 save-reading fetches, so two captures of the same save with no code change still differ by **2,485** entries. The text assertions cover what these panels **say**; nothing here covers what they **look like**. **Two process lessons paid for in full runs:** `node --test` has no working way to render a React component out of the Vite bundle — two `mc-budget` attempts died on `Minified React error #327` and `Identifier 'Sv' has already been declared` before the constraint was named, after which attempt 3 went 0/12 → 10/12 in one run; and a lane that cannot execute shell cannot verify itself, which is why the `strategic-commentary` port arrived with a failing assertion whose message (`must also be absent`) contradicted its own code (`assert.ok(text.includes(...))`) — a missing `!`, found only by running it here. |
| **`docs/react-migration-spec.md` (Phase 0: Scaffold and Prove Coexistence)** | — | **React 18 + MUI v6 + Emotion coexists with the vanilla v2 dashboard with 0 computed-style diffs and 0 backend edits.** Vite builds into `public/v2/app/bundle.js` (gitignored), loaded deferred via `<script type="module">` in `public/v2/index.html`. Harness `scripts/verify_computed_style_baseline.js` proved 100% deterministic (custom properties sorted, `#hudSnapshot` wall clock rect/width masked) and verified **0 computed style or geometry differences across all 48 matrix states** (6 views × 4 viewports × 2 modes). `tests/reactCoexistence.test.js` proves `createRoot(document.getElementById(panelId))` mounts into `VIEWS` registry panels without disrupting unmigrated panels and `assertViewRegistryIntegrity()` passes. Measured baseline production bundle floor: **302.81 KB raw / 82.69 KB gzipped**; budget enforced via `scripts/verify_bundle_size.js` (< 250 KB gzipped / < 1,000 KB raw). All 1398 tests pass. |
| **`docs/test-fixture-migration-spec.md`** | — | **Playing the game no longer breaks `npm test`.** Seventeen files that read the live save folder migrated to committed fixtures (`tests/fixtures/snapshot-player-intel.json` / `snapshot-omniscient-intel.json`, derived by `scripts/derive_intel_fixtures.js` from Autosave 4/1/2035, md5 `2e835277…`). **Bucket A (13 files):** `loadFixtureFilteredSnapshot` / `queryFixtureIntel` via `tests/fixtures/frozenSnapshots.js`. **Bucket B (3 files):** synthetic-only after live blocks moved (`holdGround`, `riskTolerance`, `snapshotLoader` resolveObserverId tests). **Bucket C (3 live suites):** `tests/live/*.live.test.js` run via `npm run test:live` — skip loudly with no save, name the save when present; a failure there is campaign drift, not a broken build. **Guard:** `tests/noLiveSaveInUnitSuite.test.js` rejects `loadFilteredSnapshot`, bare `loadSnapshot()`, or `latest: true` in any unit file. `scripts/run_unit_tests.js` excludes `tests/live/`. **Suite 1397 tests / 1395 pass / 0 fail / 2 skip** (+2 guard tests, +3 live files extracted). Deterministic across consecutive runs with the game running. |
| **`docs/post-split-cleanup-spec.md` (four of eight follow-ups)** | — | **The code index now covers CSS; one live pointer is fixed; THREAT and the two naming debts are measured and left.** *(1) `docs/code-index.md` gained a `public/v2/css/` section — 24 parts in shell link order with line counts and a parsed purpose from each header comment (not a duplicate `Purpose:` line — every part already names what it styles, and parsing couples the generator to one format rather than 24 hand-maintained strings). `tests/codeIndex.test.js` enforces parseable purposes and cascade order; `npm run index` is idempotent. Index ceiling raised 400 → 500 lines. *(2) **One genuinely stale live pointer**, not nine: `public/v2/js/components/world-map.js:38` now names `01-tokens-and-base.css` for `--fs-map-note`. The other **17** `mission-control.css` references are deliberate provenance or past-tense history (24 part headers, `index.html:10`, verify scripts, archive paths, shipped specs). *(3) **THREAT left as-is.** Measured 2026-08-24 at 1440 / 1600 / 1920 in both modes: the four half-track board tables in ALIEN FORCE POSTURE and CAPABILITY MATRIX want 728–811px in 652–732px card bodies (short 28–159px at 1600; two of four still clip 22–49px at 1920). Scroll hints match real overflow. Spanning those cards would restructure the view and roughly double THREAT height; COMMAND stays inside the 3.00-screen budget at 2.82 / 2.92. *(4) **`.mc-skill-*` and `.faction-intel-*` left unchanged** — cosmetic only; merging or renaming touches cascade order or JS/CSS/tests together for no user-visible gain. Nothing computed moved: existing `tests/missionControlLayout.test.js` and `assertStylesheetManifest()` green. **+2 tests** in `tests/codeIndex.test.js` (CSS purpose + cascade order; index ceiling 400 → 500). `verify_mobile_overflow`, `verify_drive_explorer`, `verify_mining_registers`, `verify_v2_navigation` PASS; `verify_research_tab_layout` still fails at 3.020 screens, unchanged. |
| **the v2 stylesheet split (`public/v2/css/`)** | — | **9,268 lines in one file became 24 ordered parts, and the proof is that nothing computed moved.** *The boundary rule is a slice, not a grouping.* Every part is a CONTIGUOUS range of the original in its original order; 21 of the 23 cuts land on a banner comment the author already wrote. The two seams that looked obvious were both rejected on measurement: **"one file per view" needs reordering** — COMMAND's panels sit at lines 1101, 1260, 1844, 7369 and 7552 and THREAT's at 1359, 1819 and 8959, because the file is ordered by when each surface was BUILT, not by which view it serves — and **"the measured/estimate registers as one unit" would break the cascade**, because all three pairs (`.mining-meas__value`, `.de-measured`, `.fe-meas`) sit after the rules they beat, which `tests/mineModuleOutput.test.js` asserts directly. A slice needs no argument; a move needs one, and a refactor that also moves a rule cannot be proven to have changed nothing. `16-board-skill-cells.css` is 19 lines for exactly that reason: `.mc-skill-*` belongs with `12-executive-boards.css` but the source declares it 1,500 lines later, after the responsive pass. **Nothing user-visible moved, measured against a frozen `ExitSave.gz` (md5 `5c0d9ef9…`) in a three-save folder with pinned mtimes.** 64 page states — six views x 375/1440/1600/1920 x player/omniscient, plus the faction-intel and intelligence-library overlays at every width in both modes — capturing the FULL `getComputedStyle` map for every element and both its `::before` and `::after` pseudos, plus every `getBoundingClientRect`: **128 of 128 state dumps byte-identical**, and **64 of 64 screenshots pixel-identical** at threshold 0. The only DOM change is 23 extra `<link>` nodes in `<head>`, which have no box. **Two identical baseline runs proved the harness deterministic first**, and doing that surfaced two things a naive harness would have mistaken for regressions: Chromium does not enumerate CUSTOM properties in a stable order between page loads (8 of 469 transposed between identical runs, same values), so the dump is sorted; and `#hudSnapshot` renders a wall clock, so its own rect is masked out of the pixel comparison — 7,479 masked pixels between the two baselines, 8,162 across the change, and zero differing pixels outside it in either comparison. **A byte proof backs the visual one**: the 24 parts minus the 11-line header each gained concatenate to the original **exactly**, 207,942 bytes. **Token counts are all unchanged** — 1,271 rule blocks, 1,300 selectors, 4,521 declarations, 17 `@media`, 82 `!important`, 65 custom properties, 389 `font-size`, 1,826 `var()`, 26 pseudo-elements, 1 `:root`; the only figure that moved is total bytes, +13,860, which is the 24 headers. The browser assembles **1,170 CSSOM rules from 24 sheets**, the same 1,170 top-level blocks the single file had. **`<link>` tags, not `@import`**: links are discovered while the HTML is parsed and fetched in parallel, whereas each `@import` is only discovered after its parent has been downloaded and parsed, serialising 24 round trips in front of first paint; `public/index.html` already links its two stylesheets the same way, and nothing in these files uses `url()`, so no part's relative-URL base changed. **The six direct readers now read the set, not a path.** `tests/fixtures/missionControlCss.js` takes the order from the shell's `<link>` tags — the one place the browser takes it from — and `assertStylesheetManifest()` checks that set against the directory in both directions, so a part added later is covered with no test edited and a part added WITHOUT a link fails loudly instead of going unstyled and unscanned. **Four deliberate mutations, each confirmed red, and the probe was enumerated LAST rather than first** so the retarget is actually tested: a `font-size: 11px` literal in it failed `typeScale` naming `public/v2/css/99-probe.css:3`; a `.probe-scroll-hint` class in it failed the scroll-hint registration check; the same file with no `<link>` and a `<link>` with no file each failed the manifest check. **A fifth mutation proves the ORDER is load-bearing and that the harness sees it**: moving `05-view-grid.css` after `15-responsive.css` reverts `.init-view__grid` from `display: flex; align-items: stretch` to `display: grid; align-items: start` at 375px, grows the page 2,172 → 2,244px and turns the holo-node grid from 2 columns back to 5 — the DRIVES-clipping bug that block exists to fix. A sixth, swapping `15` and `16`, came back GREEN, which is honest evidence that `.mc-skill-*`'s position is not load-bearing at that state — measured at one state, not proven at all of them, which is why it stayed a slice. **Suite 1397 → 1398 tests, 1395 → 1396 passing, 0 fail, 2 skips unchanged** (the second is the absent `dist/`); the one new test is the manifest check. `verify_mobile_overflow` PASS with COMMAND at **2.82 player / 2.92 omniscient** screens at 1920 — the documented figures to the digit — `verify_drive_explorer`, `verify_mining_registers` and `verify_v2_navigation` all PASS with 0 console and 0 network errors. `verify_research_tab_layout` still fails at **3.020 screens / 3,262px omniscient**, byte-identical to its failure at the base commit and not this change's |
| **the v2 type scale (`public/v2/css/mission-control.css`)** | — | **24 rendered sizes became 7, and four of the 24 had never been written down anywhere.** *Before, measured in the live DOM (Playwright, `ExitSave.gz`, 1440px, both modes, per-view over text-bearing elements):* COMMAND **16** (8, 8.25, 8.5, 9, 9.17, 9.5, 10, 10.5, 11, 12, 12.5, 13, 15, 18, 21.6, 30), EXPANSION **14**, THREAT **10**, RECORDS **6**, FLEET **5**, DRIVES **4**. *After:* COMMAND **9**, EXPANSION **5**, THREAT **5**, RECORDS **5**, FLEET **4**, DRIVES **4** — **identical in both modes**, and COMMAND's 9 is 7 page tokens plus the world map's 2 SVG user units. DRIVES was the target and did not move: 2,010 text-bearing elements, four sizes, which is what proved the dense tiers sufficient rather than merely tidy. **393 `font-size` declarations, 310 of them raw literals; 300 rewritten to a token, 10 deleted as redundant, 0 literals left.** **Where the fractional sizes came from is the part worth reading:** 8.25, 8.33, 9.17, 9.58 and 10.83px are all 5/6 of something else — an unstyled `<small>` takes `font-size: smaller` from the UA sheet — so five rendered sizes existed that no rule declared and that changed depending on which element the markup nested inside. `small { font-size: inherit }` now makes every step down deliberate; measured first that **every `<small>` already rendering at a token value had an explicit rule**, so the reset only reaches the fractional ones, and the six that needed a step got one. 21.6 and 30.24px were the other two: `clamp(18px, 1.5vw, 22px)` and `clamp(21px, 2.1vw, 30px)`, the only fluid type in the file, so the rendered size depended on window width. The display tiers are **fixed on purpose** — this is an Operate surface, and a vw clamp is how a size reaches the page without being written down. **The scale is 9 / 10 / 11 / 12.5 / 15 / 19 / 26**, ratios 1.111 / 1.100 / 1.136 / 1.200 / 1.267 / 1.368; the dense four are unchanged, the display three replace fourteen values between 14 and 30px (24/25/26/27 were four different titles). `body` was **13px, off its own scale**, and is now `--fs-row` — that alone collapsed a whole tier, because most of COMMAND's 13px text was inherited rather than declared. **`--fs-label` was referenced twice and defined nowhere, and that is not a no-op:** an undefined custom property is valid at parse time and invalid at computed-value time, so the declaration **wins the cascade and then resolves to `inherit`** — FLEET's procurement heading rendered at the body's 13px while out-specifying the 10px `.ra-procurement__head` rule beside it, on an element that carries both class names precisely to say the two components are the same. **Inter was declared first in `--sans` and never loaded** — no `@font-face`, no `<link>`, and `mission-control.css` is the only stylesheet the v2 shell pulls in — so it is **removed, not shipped**: adding a webfont would change every measured width and height in the same commit that changes the sizes, and a reader who happened to have Inter installed locally was already getting a different dashboard from everyone else. **The world map is deliberately NOT on the scale.** Its `<svg>` is `viewBox="0 0 720 360"` at `width: 100%`, so a size inside it is a user unit scaled by container width — 8 units renders near 7px in a 640px card. It ran **five** values (8, 8.25, 8.5, 10, 10.5) for two roles, with 8 vs 8.25 (3%) separating a theater's fleet count from the status line above it; now a named two-step ladder (`TYPE` in `world-map.js`, `--fs-map-name` / `--fs-map-note` in `:root`). **No figure changed:** rendered text of all six views in both modes captured before and after and diffed whitespace-ignored — **12 of 12 identical**. **Distinctions that collapsed were counted, not assumed:** every parent/child and adjacent-sibling pair of text-bearing elements whose sizes differed was captured before (**127 pairs**) and after; **12 collapsed**, 10 of them already carried by weight, colour, family, style, tracking or case, and the two that were not (`button` vs `output` in the MC hull stepper) are a bordered 22×22 control against a bare numeral. **Four distinctions were restored rather than left flat.** (1) `.mc-board-secondary` was 8px under a 9px cell — below the floor the file documents — so it joins the tag tier and its gap widens one step; growing the cell to `--fs-meta` instead was **measured and rejected**, because it is an 11% widening of the densest tables in the app and their scroll hints are measured against natural widths (621–784px). (2) `.alien-hate-econ-section-heading small` (0,1,1) **out-specified `.alien-hate-econ-status` (0,1,0)** and painted BOTH GATES CLOSED the same grey as an ordinary footnote — a status designed to go green or amber that rendered dim either way; with both halves now on the tag tier the state colour is what separates them, and does. (3) `.directive-risk-held-title` gains `font-weight: 600` where 13-over-12 used to stand in for it. (4) `.fp-procurement__head small` lost its size so the FLEET and research-advisor procurement heads render identically again. **Five declarations sat at 7px and three at 8.5px, i.e. beneath the 9px floor the stylesheet itself documents;** they are raised, which is why THREAT's qualifiers ("157.1 to go", "ceiling, grows yearly", "requires raw hate") are legible for the first time. **`scripts/verify_mobile_overflow.js` PASS — 0 unreachable at 375/414/768 across all six views in both modes**, `rawOverflow` counts byte-identical to the baseline (DRIVES 1934/841, THREAT 127/114, all inside scrollable ancestors). **One regression was caught and fixed by it:** `.init-sector-badge` was `flex: 0 0 auto` and survivable only at 8px — "CONTESTED (1 Hostile the Servants Executives)" is 44 characters and ran **386px against a 375px viewport with no scrollable ancestor**; it now shrinks and wraps inside its border (measured right edge 333px), which keeps the faction name rather than ellipsising it. **COMMAND at 1920: 2.85 → 2.82 (player), 2.90 → 2.92 (omniscient)**, both inside the 3.00 budget. **Reported not fixed:** `scripts/verify_research_tab_layout.js` measures COMMAND differently and **already failed at the base commit `3660c98`** — 2.949 player / **3.006 omniscient** before, 2.921 / **3.020** after. The +16px is attributable: **+24px is the eight SITREP paragraphs going 12 → 12.5px**, against −20px from the hero KPIs (30 → 26), −14px from the threat board and −9px from the priority brief. Buying it back means content or layout, not a smaller scale — the SITREP runs about **105 characters per line** at 1440 against the 45–75 comfortable range, and fixing that measure makes the block **taller**, not shorter. `verify_mining_registers.js`, `verify_drive_explorer.js` and `verify_v2_navigation.js` all PASS, so the measured-vs-estimated registers on `.de-table`, `.fe-table` and mining are intact — none of them was ever carried by size, and the mining estimate band's gap over its measured neighbour **widened** (11.5/9.58 → 11/9). New `tests/typeScale.test.js` (4 tests) pins that every `font-size` names a token, that every token named is defined in `:root`, that the seven steps are monotonic with no step under 9%, and that no component script writes an inline `font-size` literal; **all four were broken deliberately first and each failed with the expected diff**. The inline-literal check earned its keep immediately — it found **five** more `font-size: 9px` in `strategic-commentary.js` and **three** in `mission-control.js` (including a `size < 48 ? '8.5px' : '9.5px'` conditional on the GDP bubbles) that the stylesheet could not see at all. **Honest downsides:** (1) COMMAND still renders 9 sizes, because the map's two user units are counted and should be — they are a different coordinate system, and making them coincide with token values would be gaming the metric. (2) The omniscient COMMAND height is 16px worse on the stricter script, which was already over budget. (3) `.fp-procurement__head` drops 13 → 10px, a visible shrink, which is the correct value and not the familiar one |
| **the 1400px inversion, and COMMAND's right column against THREAT's pattern** | — | **Widening the browser by 41px cost the operations table 316px, and COMMAND printed two of its figures twice.** `@media (min-width: 1400px)` gave `.operative-leaderboard` `repeat(auto-fill, minmax(280px, 1fr))`; `auto-fill` does not know the panel is already inside COMMAND's half-width column, so at 655px it fitted **two** 280px tracks and split the panel again. **Measured in the live DOM (Playwright, `ExitSave.gz`, both modes):** *before* — 1399px gave the 780px 12-column table a **639px** window (81.9% visible, 141px hidden); 1440px gave it **323px** (41.4%, 457px hidden), computed `grid-template-columns: "322.5px 322.5px"`. *After* — 1399px **639px**, 1440px **659px** (84.5%, 121px hidden). Widening now gains the table 20px instead of costing it 316. **Moving the card to `.init-view__span` was measured and rejected:** it takes COMMAND at 1920 from 2.80 to ~3.29 screens and `scripts/verify_mobile_overflow.js` caps that at 3.00. The right column was then rebuilt against THREAT's full-width-metric-strip pattern: the executive strip moved out of the PRIORITY BRIEF card (it sat **above** that card's own header, the only body→header→body card on the page, and at 165px per cell it rendered "STRATEGIC SCORE (EST..." and "Composite estimate · Ra..."); the duplicate `#hudPower` HUD pill went (it was also the **unguarded** instance — `${powerScore}/100` with no null check, so an absent score would print "null/100", while the strip prints UNAVAILABLE and carries the rank the pill never did); the primary directive title stopped printing twice (`primaryDirective()` builds the statement as `recommendation \|\| title` and the engine emits no `recommendation`, verified against `/api/v2/briefing` in both modes); and the vestigial decoration went — the `display:none` orbit rings, the six hardcoded "01".."05" numerals, the 42%-wide `::before` accent rule that connected to nothing, the nested bordered "card inside a card", and `cursor: pointer` on a div with no handler, tabindex or role. The 38px directive title, the largest element on the page and a **label**, came down to `clamp(18px, 1.5vw, 22px)` — under the strip's measurements, where THREAT's 19px values sit. **No figure changed:** COMMAND's rendered text was captured at 375/1399/1440 in both modes before and after and diffed; every numeric token matches as a multiset except the six decorative numerals. `verify_mobile_overflow.js` **PASS** — 0 unreachable at 375/414/768 across all six views in both modes; COMMAND at 1920 is 2.85 (player) / 2.90 (omniscient) screens, up from 2.75/2.80 and inside the 3.00 budget. New `tests/commandLayout.test.js` (7 tests) asserts the monotonicity, the measured scroll hint, the single score, the un-repeated title, header-first cards, no vestigial decoration, and no clipped figure; **all nine deliberate breaks confirmed red first**, and B7 changed the test — it went green at 375/1440 because the node status only clips at 1200/1320/1399, which a 14-width sweep found and the test now covers. |
| **two width-specific layout defects (`public/v2/css/mission-control.css`)** | — | **Both were invisible at 1440 and 1920 and broken in between, which is why source review and a browser pass at 1440/375 missed them.** *(1) The header collided with itself from 1501px to ~1690px.* `.init-topbar` switched to a three-column grid at 1501px whose own minimum was 1625px — 220 + 320 + the controls' **989.4px measured max-content** third track + 44 gap + 52 padding — so both `fr` tracks clamped to their floors and the brand's 291px of content escaped a 220px track. Measured across 41 widths from 1240 to 2040: **9 colliding, 1520–1680 sampled, worst 27.8 × 6px**, "CAMPAIGN INTELLIGENCE / EXECUTIVE BRIEFING" over the ALIEN HATE meter. Fixed by making the header a **wrap row** rather than three breakpointed grids: the controls now drop to their own row exactly when they stop fitting, measured between 1700 and 1720px. **After: 0 of 41 collide.** The 1500 and 1320px blocks were hand-approximating that and are gone; the 1200px one was **not** an approximation and is kept, restated as `flex-basis: 100%` below 1240px — deleting it cost four quietly ellipsized HUD readings at 1024px (OBSERVER, the faction name, STRATEGIC SCORE (EST.) and its value), because the HUD's four-column grid needs 858px and sharing the brand's row leaves 659px. Header height is **unchanged from the old grid at every measured width except three**: 1240px 211 → 250 (that own row buys the four readings back), and 1600/1700px 171/151 → 188. A second, independent fault — `.init-brand-sub`'s `text-overflow: ellipsis` could never fire, its right edge a constant 316.9px at all 41 widths — was **not** fixed by clipping (that hides the measurement and leaves the text unreadable) but by letting `.init-brand-name` wrap; with a 68-character faction name and `white-space: nowrap`, the brand ran **411px past the header at 375px with no scrollable ancestor**. *(2) Grid tracks starved the content in them.* `@media (min-width: 1600px)` gave `.init-view__grid` three tracks; **no view has three non-span cards** (EXPANSION/THREAT/RECORDS have two, FLEET/DRIVES one), so the third was always dead and the two real cards paid 764px → **501px**. Track count now follows card demand — `repeat(auto-fit, minmax(min(100%, 560px), 1fr))` — and 3 × 560 + 2 × 24 = 1728 > the 1612px content box the 1660px max-width allows, so a dead track is now **structurally impossible**. Inside RECORDS, `.mc-watch-grid` was a hard three-up needing 1448px inside a half-track card: **144px per section at 1600px against 439/425px tables**, clipping 316px each. Now container-driven. Third, `.mc-board-table { min-width: 780px }` was **slack above the content, not a legibility floor** — every table under it also carries `white-space: nowrap`, whose min-content width already prevents squashing, and the two modifiers that enable wrapping (`--ledger`, `--fleet`) both replace it with `min-width: 100%`. Measured natural widths **621, 710, 742, 749, 775, 784px**; removed. **Clipped scrollers at 1600px, both modes: EXPANSION 1 → 0, RECORDS 2 → 0, THREAT 4 → 4 but shortfall 137–315px → 10–52px (now genuine content demand, correctly announced), COMMAND 417px → 347px (improvement only; the wrapper itself is another agent's).** Rendered text of RECORDS and EXPANSION diffed whitespace-ignored in both modes: **the only change is the removal of three now-false "SWIPE HORIZONTALLY TO VIEW ALL COLUMNS" hints. No figure changed.** `scripts/verify_mobile_overflow.js` byte-identical before and after — 0 unreachable at 375/414/768 in both modes, COMMAND 2.75/2.80 screens. New `tests/missionControlLayout.test.js` sweeps 940–2040px for header overlap **and for silently ellipsized HUD readings**, checks a 68-character faction name from 320px up, pins track count against non-span card count, pins the watch tables against their column, and checks every scroll hint **two-sidedly** against real overflow; all six guards were broken deliberately first and each failed with the expected numbers. One of them earned its keep immediately: the first version of the long-name guard **passed with the containment removed**, and rewriting it to measure the narrow band showed both that the guard was vacuous and that `overflow: hidden` would have hidden the measurement rather than fixed it. **Still open, reported not fixed:** THREAT's two table cards want 782/784px and get 732px at 1600px — closing that means making them full-width spans, which restructures the view rather than fixing a defect. |
| **bench displacement reasons (tracker item 6, third pass)** | — | **The bench gave the wrong reason on all sixteen live rows, and the right reason was already computed.** Every row not held by the risk floor read `Displaced by ${assignments[0].councilor.name} assigned to direct high-priority mission.` — the FIRST assignment's councilor, named regardless of whether that operative had anything to do with the candidate. **Measured 2026-08-23 on frozen `ExitSave.gz` (md5 `5c0d9ef98213c91d8187ae11bf885d57`), harness proved deterministic across two identical baseline runs.** *Omniscient, before:* all 8 rows said "Displaced by Hemaraj Pavanaja". *Omniscient, after:* all 8 say **"Displaced by the alienHate budget, not by a busy councilor — it charges 4.57 hate against 3.16 left of a 7.90 cycle cap (4.74 already committed), 1.41 short. Mahangeet Pakimor was free to run it, so freeing another operative does not make this affordable."** The engine had computed the pool, the charge and the shortfall and discarded all three, and even the councilor was wrong — `budgetDisplaced` names **5799 Mahangeet Pakimor**, not 5797 Hemaraj Pavanaja. **Player mode was a larger lie than the brief anticipated and is the part worth reading twice:** all 8 rows blamed **Beth Hofmann**, who was the **worst-scoring operative on every one of the eight candidate lists** (−13.95 to −25.04). The measured truth is that **7 of the 8 had EVERY way of running them refused as a value-destroying switch** (now: "Held by active commitments — all 6 ways of running it would break a posting worth more than the mission returns; the closest is Brad Lester at net −0.81") and the **eighth was genuine contention, by Brad Lester and Mahangeet Pakimor** — never by Beth Hofmann. **Which reason binds is a stated rule, not an accident of loop order:** the obstacle NEAREST TO TAKEABLE, so budget > contention > switch-rejected > risk floor, with `mixed` and `no-priceable-operative` as their own outcomes and `undetermined` reachable and stated in words. Budget outranks contention because **a refused pairing had already passed every other gate** — its councilor was free, unclaimed, above the floor and not switch-rejected — which is exactly what makes "free up a councilor and I could do this" the wrong conclusion. **The owner's actual question is now answered: `benchBudget.jointlyAffordableCount` is 0 of 8** — the eight rows share one 7.90 hate cap with 3.16 left and each charges 4.57, so none of them can be added to the committed plan. It is computed cheapest-first (the LARGEST honest count for one pool) and labelled an **upper bound**, because only the pool that refused each row was priced. **A row the budget never evaluated is UNPRICED, counted neither as fitting nor as refused**, and a bench nothing refused reports **NOT COMPUTED** with the reason, never "all of them fit". **A player-mode defect found while checking the brief's own question:** the hate cap **is** computable there (8.5 against omniscient's 7.9) but it is derived from `minimumAlienHate` — the Mission Control **floor** — because `actualAlienHate` is redacted, so it is an **UPPER BOUND on the real budget** while `capMeasured: true` said only that a number came out. Refusing the cap was considered and **rejected**: it would make every player-mode affordability check unevaluated and so **move the plan**, and an unbounded hate budget is worse than an optimistic one. `currentHateBasis` (`measured` / `floor` / `null`) and `capIsUpperBound` are reported instead, and both surfaces print the caveat. **`groupBudgetDisplacedCount` mirrors `groupRiskFloorHeldCount`** so a collapsed row never presents one member's reason as the group's; the board raises a "Mixed group: N of M" caveat only when the count disagrees with the row's own verdict. On this save all 33 represented candidates are budget-refused, so every group is uniform and no caveat fires. **War-room §10 gained two lines** — the pool's own state with its basis, and the joint-affordability count — costing player 28,384 → **28,827** and omniscient 27,875 → **28,281** (**+443 / +406**) against the 30,720 budget, leaving **1,893 / 2,439** of headroom, nothing dropped. **Proof the plan did not move: with the new fields removed and `displacedBy` blanked, the ENTIRE briefing is byte-identical before and after in BOTH modes**, and the war-room markdown is identical once the two new lines are removed. **31 new tests, 14 deliberate mutations each confirmed red** — including one that restores the councilor sentence, one that lets the board fall back to it, one that counts every priced row as fitting, one that misfiles a budget-refused pairing as contention, and one that lets a floor-derived hate cap claim to be measured. **A first mutation did NOT fire and that was a real gap:** deleting the `unclassified` bucket from the obstacle tally changed nothing, because the bucket is provably empty for every reachable input — so the tally test was strengthened to assert that the tally AGREES with the stated cause (a `budget` cause requires a counted refusal; a `contention` cause requires none), which catches a misclassification that still reconciles. **Honest downsides:** (1) `budgetChecksUnevaluated` now also collects unmeasured pools seen on the refusal path — a completeness fix consistent with the field's documented meaning, but it is a widened definition, and it is empty on this save either way. (2) The `chargeMeasured === false` guard in the joint-affordability filter is **defensive and currently unreachable**: the hate pool only refuses a positive measured charge, so no live row can exercise it. (3) A budget-refused row's sentence is long (about 250 characters) and the board renders it in full. (4) `hateCharge` in the greedy pass still coerces an unpriceable hate row to **0** — pre-existing, out of scope here, and now at least visible through `chargeMeasured` |
| **bench diversity (tracker item 6, second pass)** | — | **Closes the variety regression the first pass measured and refused to smooth over, and the board's second cap with it.** The bench now selects the highest-scoring eight **GROUPS of siblings**, one row per group, each row carrying how many candidates it stands for. **Grouping key = `missionType` + the coarse target entity** — the nation, hab, councilor or faction the mission acts on, not the individual control point inside it. **Measured on frozen `ExitSave.gz` (md5 `5c0d9ef98213c91d8187ae11bf885d57`), harness proved deterministic across two identical baseline runs before and two after.** *Omniscient, before:* **2 distinct mission shapes across 8 rows** — five 68.75 "Purge the Protectorate hold on … in China" siblings of the primary recommendation itself, then three 50.64 India purges — accounting for 8 of 427 candidates. *Omniscient, after:* **8 / 8 distinct shapes** over the same eight rows — Alien Administration 42.19 (+4), Brazil 27.83 (+3), China 68.75 (+4), European Union 44.16, India 50.64 (+4), United Malay Nation 37.65 (+4), Russia 30.52 (+3), United Kingdom 26.51 (+3) — **accounting for 33 of 427**, and the best candidate no row stands for falls from **50.64 to 23.74**. *Player:* **nothing collapses** — 46 groups from 46 candidates — so the eight emitted rows, their order and their scores are **identical to before**, and the only change to the payload is the group fields (all `groupCount: 1`). **Three grouping rules were measured and two rejected.** `family + coarse target` produced *identical emitted rows*, so there was no measured basis to prefer it, and it merges `Investigate Councilor` with `Turn Councilor` and `Control Nation` with `Purge` — two genuinely different actions presented as one option. Adding the holder faction drops to 6 / 8 shapes for no gain, because **the holder differs INSIDE 3 of the 7 collapsed groups** (India = Servants + Protectorate, United Malay Nation = Protectorate + Project Exodus, Russia = Protectorate + Resistance) — which is also why `groupNote` describes the GROUP and names only the mission and the target, never the representative's own holder. `missionType` alone gives 6 groups total, one spanning 68.75 down to −1.77. **The counts do not change meaning.** `benchedTotalCount` and `benchedOmittedCount` still count the whole bench and rows-not-carried (8 of 427 · 419 omitted; 8 of 46 · 38 omitted), so `benched.length + benchedOmittedCount === benchedTotalCount` still holds and the four existing tests that pin it are untouched. The new figure is a **third** one beside them: **`benchedRepresentedCount`**, how many candidates the carried rows account for. **Absent stays null throughout** — an unreadable group key makes a record its own group of one and never a shared "unknown" bucket (an unreadable key is not a shared key), `councilorName` is **not** a fallback for a missing `councilorId` because two councilors can share a name and merging them would fabricate an identity (measured: **33 of 33** omniscient council candidates carry the id, so requiring it costs nothing), and a group whose members could not be scored reports `groupScoreLow` / `groupScoreHigh` as **null, never 0**. `looksUnresolved` **moved** to `shared/util.mjs` and is re-exported from `server/engine/candidates/normalize.js` as the **same function object** — asserted in a test — rather than copied, which is how `sameId` once split into two rules that disagreed. **The board's `BENCHED_RENDER_LIMIT = 5` is deleted** and the board now renders every row the engine hands it; the comparator was **not** duplicated in browser ESM (these components are classic `<script>` tags and cannot import `shared/*.mjs` at all) — the fix was to stop deciding. **War-room §10's sentence was rewritten, not appended to**, and the two claims an existing test pins — "HIGHEST-SCORING few" and "ties broken by candidate-generation order" — are kept **verbatim and still true** because they now scope to groups; player 28,099 → **28,384** bytes and omniscient 27,588 → **27,875** (**+285 / +287**) against the 30,720 budget, leaving **2,336 / 2,845** of headroom, nothing dropped. **Proof the plan did not move: with every bench-shaped field blanked, the ENTIRE briefing is byte-identical before and after in both modes** — the only difference in the whole capture is the new `benchedRepresentedCount` key itself, and with that one line removed from both sides the blanked briefings MD5-match exactly. `/latest-snapshot.md` and `/latest-threats.md` are byte-unchanged in both modes. **23 new tests, 22 deliberate mutations each confirmed red** — including one that removes grouping, one that gives every ungroupable record a shared key (which goes red on **six existing tests as well**, proving the old fixtures really are singletons), one that lets `councilorName` stand in for a missing id, one that emits ranked instead of generation order, one that turns a null score range into 0, and three against the war-room sentence. **One mutation deliberately did NOT fire and that is the correct result:** shuffling the groups before ranking changes nothing, because `compareBenchSelection` is a total order (score, then generation index, nulls last) — so the input order provably cannot reach the output. **A first attempt at the "literal `undefined`" mutation also did not fire, and that was a real gap:** on a `controlPoint` the scope id and the scope label read one field, so a test using only that kind proves whichever check runs first and nothing about the other. The test now uses `nation`-kind cases where `id ?? name` and `displayName ?? name` diverge, and both halves are independently pinned. **Honest downsides, stated rather than smoothed over:** (1) the omniscient bench is now **8 distinct targets but still one distinct MISSION** — every carried row is a Purge, because on this save every high-scoring alternative is; the key includes `missionType` but nothing forces mission variety, so a reader asking "what other KINDS of action are open to me" still is not answered. (2) A collapsed row **names the count and the score range but not the individual siblings**, so a reader who wanted the specific control point now gets one of five and a count. (3) **Player mode gains nothing** — 46 singletons — while paying the full +285 bytes of §10 prose. (4) The war-room bullet is now a long sentence; the previous pass cost +143 bytes and this one costs twice that |
| **bench ordering (tracker item 6)** | — | **Shipped as recommended — and the measurement says it is an improvement in player mode and a regression in variety in omniscient, which the owner should weigh rather than have smoothed over.** The cap now SELECTS the highest-scoring eight and then EMITS them in candidate-generation order, so the carried list is the best few while registry emission order — which is load-bearing for how explanations are built — is untouched. `applyRules` and `scoreCandidates` were not reached. **Measured on frozen `ExitSave.gz` (md5 `5c0d9ef98213c91d8187ae11bf885d57`), harness proved deterministic across two identical baseline runs.** *Player, before:* 5.63, 10.24, three 2.25 Investigates, 9.0, two 3.0. *Player, after:* 5.63 Madagascar CP, 10.24 Defend Interests, 9.0 alien-detection conversion, then four Advise rows at 4.0–4.12 — the three genuinely best actions are all on screen and the 2.25 filler is gone. *Omniscient, before:* 5.63 Madagascar, six score-3 Investigate/Turn rows on three councilors, 9.0. *Omniscient, after:* **five 68.75 "Purge the Protectorate hold on … in China" and three 50.64 India purges.** That is truthful — the best hidden alternatives really were 68.75, and a reader shown the old bench would conclude their options were all score-3 investigations — but the eight are now five siblings of the *primary recommendation itself* (`Purge … ExtractiveSector in China`, 68.75), so the bench reads "five more of the thing you were already told to do". **Score-selection alone cannot fix that; a per-target or per-mission-family diversity rule would, and is deliberately NOT in this change.** Ties break on generation index (the common case: **39 of the 427** omniscient entries score exactly 3) so two runs of one save cannot disagree. An unreadable score sorts LAST, never as 0 — measured **0 of 427 and 0 of 46**, so it is a guard against a shape that does not occur today rather than a live correction; the emitted `score` field keeps its existing display contract unchanged. **`benchedTotalCount` / `benchedOmittedCount` still count the whole bench** (8 of 427 · 419 omitted; 8 of 46 · 38 omitted). **War-room §10's ordering sentence was false the moment this landed and was rewritten**, not appended to; player 27,956 → 28,099 bytes and omniscient 27,445 → 27,588 (**+143 each**) against the 30,720 budget, 2,621 / 3,132 of headroom, nothing dropped. **Proof the plan did not move: with the bench array blanked, the ENTIRE briefing is byte-identical before and after in both modes** — same primary, same five/three assignments, same `totalExpectedValue` 66.13 / 19.3 — and `/latest-snapshot.md` and `/latest-threats.md` are byte-unchanged. 5 new tests, **7 deliberate mutations each confirmed red**, including one that reverts selection to a generation-order slice and one that stops the emission being generation order — they fail *different* tests, so the two properties are independently pinned. ~~**Known and not fixed:** `BENCHED_RENDER_LIMIT = 5` in `public/v2/js/components/directive-board.js` is a second cap that still slices the engine's eight in generation order.~~ **BOTH open items above — the omniscient variety regression and the board's second cap — were CLOSED on 2026-08-22 by the "bench diversity (tracker item 6, second pass)" row at the top of this table.** The per-target diversity rule this row said would be needed is the rule that shipped; the render-side cap is gone and the comparator moved into `shared/benchSelection.mjs` |
| **the rival control-point cap unlocked in player mode (tracker item 7, third pass) — AN OWNER'S INTEL-MODEL DECISION, NOT A BUG FIX** | — | **`7174764` refused a rival's cap in player mode on three grounds and `4f2f5b1` kept it; the dashboard's owner decided on 2026-08-22 that the faction CP cap does not need to be locked behind omniscient.** The refusal was correct code implementing a stricter model than its owner wants, so it is recorded as a decision in three places — the header of `shared/controlPointCap.mjs`, the faction projection in `server/intelligenceFilter.js` and the spec — precisely so a later reader does not "restore" it as a leak fix. **The unlock is exactly one basis wide.** `recorded` reads `history_CPCapOverageByDay` and **composes nothing** — no councilor attribute, no hab module, no cap project — so publishing it unmasks no input, only a conclusion the save states outright; `composed` still refuses for a rival because its terms genuinely are unreadable, and councilor attributes stay masked. **Player mode, before → after:** the Protectorate `unknown` → **−34.34, `over-cap`, basis `recorded`**, identical to the omniscient figure, worth **1,179 Influence/year** to them and **+10.64** to every hostile Crackdown / Purge / Enthrall Elites / Dominate Nation (the 32-day window mean the game applies, not today's 11.45); the observer unchanged at 237.70 `composed`; the Aliens unchanged at the hard-coded 20000; **five rivals still `unknown`.** **The floored zero was the thing to get right.** Each slot is `max(0, cost − cap) × multiplier`, so a recorded **0 is the floor** — it bounds without locating, and for the aliens it does not even bound (they read 0 while 10–11 over their MC capacity). Publishing the recording made `recorded.available` true with that zero, and the refusal branch's old `overCap: recordedAvailable ? false : null` would have flipped **six rivals from an honest `null` to a confident "not over cap" as a pure side effect**. `overCap` is now `null` wherever the headroom refuses, `recordedCapPosition` classifies every recording `position` / `bound-only` / `nothing`, and the new `boundOnlyCount` lets a consumer tell "not recorded over cap, magnitude unknown" from "nothing known". **`assertPlayerSnapshotSafe` and the whole-payload leak scan were RETARGETED, not deleted** — still throwing on a rival's `controlPointMaintenanceEffects` and raw councilor `attributes`, and the tests now assert **both directions** so a silent re-redaction fails a test. **A test instrument was corrected by measurement:** hunting the composed inputs by effect NAME finds **68 hits in a clean player payload** (the observer's own row, its `capabilities.activeEffects`, and 64 in the static `techTree` catalogue that lists which projects *grant* each effect) — the names are a shared vocabulary, so the scan is now structural. **`shared/markdownExports.mjs` closes the gap the second pass left open:** war-room §7 carries the observer's headroom with its accuracy caveat, the over-cap rival with its bill and mission bonus, and an explicit announcement that a zero bounds without locating — **1,077 bytes player / 748 omniscient against a 30,720 budget with ~6 KB of slack, nothing dropped to fit it**. **Omniscient did not move:** the snapshot is byte-identical, the endpoint diff is **purely additive** — not a line removed or reworded, all 2,588 pre-existing numbers unchanged — and the engine briefing, `/latest-snapshot.md`, the full report and `/latest-threats.md` are byte-identical in **both** modes — the only differing files in the whole capture are the two war-room exports, both purely additive. Captured against an MD5-verified frozen `ExitSave.gz` with two identical baseline runs proving the harness deterministic. **6 new tests, 12 deliberate mutations each confirmed red** |
| **the shipyard throughput model** | — | **Settled by measurement: serial within a yard, parallel across yards — and the dividend was wrong as well as the divisor.** Measured against four MD5-verified frozen saves at three campaign moments (`Autosave3` 12/1/2034 d3225, `Autosave2` 12/16/2034 d3241, `Autosave`/`ExitSave` 1/1/2035 d3256) and all eight factions. **The save's `nShipyardQueues` is a map keyed by SHIPYARD MODULE**, an entry array per yard, which `buildShipyardQueues` flattens per-ship — so `ownQueuedShips.length` was never a count of yards. Across **129 yard-queues carrying work, ZERO had more than one entry with `costPaid`**, and every paid entry sat at index 0; over both intervals **all 15 unpaid entries held `daysToCompletion` exactly frozen** (Humanity First's yard 17063 sat at 60 / 60 behind a head running 111.80 → 95.80 → 80.80) while paid entries counted down by exactly the elapsed campaign days. So a yard builds one hull at a time and yards run concurrently, and the divisor is now `concurrentBuilds`. **The observer's own number did not move** — five hulls at five separate yards, so 1.25/mo before and after in both modes — but on the same save the Servants ran **15 queued against 5 building (3.00× overstatement)**, the Protectorate 5 against 3, and **the Academy had 2 queued and 0 building**, which the old model published as two hulls' worth of parallel throughput from a faction delivering nothing. **The dividend is worse.** `baseConstructionTimeDays` is not the build time: `TIHabModuleTemplate.json` carries `constructionTimeModifier` (SpaceDock 1.0, **Shipyard 0.8**, Spaceworks 0.6; and non-yard modules too — ConstructionModule 0.9, Nanofactory 0.75), and `TIEffectTemplate.json` carries four `Effect_ShipConstructionTimeReduction*` variants (×0.8 / ×0.9 / ×0.95 / ×0.9875). **The observer HOLDS `Effect_ShipConstructionTimeReduction` (×0.8)** — read from `TIEffectsState.factionEffectsNames`, which is a map of context → effect names, not a flat list — and runs **11 `Shipyard` modules beside 3 `SpaceDock`s**. Against the waiting entries that state their own duration, the ratio to the template base runs **0.30–0.86 across five factions on one save**. Every modifier only shortens the build, so the template-derived rate can only be too low: it is now published as **`AT LEAST 1.25 × Monitor per month`** with `throughputBound: 'lower'` and `buildTimeBasis: 'hull-template-base'`, and a non-refit queued hull of the target type states its own duration in preference (`'measured-queue-entry'`). **The save states the answer, so the export prints it rather than reconstructing it**: `daysToCompletion` on a building hull decrements by exactly the elapsed days, so a new **MEASURED** pipeline line carries `5 hull(s) building, 0 waiting behind them — 5 of 14 shipyard(s) working, 9 idle. next in 21.2 days, all 5 inside 25.2 days; 5 due inside 30 days`. **A station is not a shipyard**: the observer's 14 yard modules sit across only 6 habs, five of them on Nearchus Station alone, so the count is of `habModules[].isShipyard`, never of habs. **Null discipline**: an unread queue refuses as before; a queue row with no readable `constructionStatus` makes the concurrency **unknown rather than smaller** (the `Number(null) === 0` failure wearing a filter); an unreadable `daysToCompletion` sets `deliveriesWithin30Days` to null rather than shrinking it; an absent `habModules` gives a null yard count, not zero yards; and an unreadable build time kills the **rate only** — the measured pipeline still reaches the reader. **Consumers**: only three — `server/commentary/simulation.js` produces it, war-room §11 and the COMMAND card render it. **No directive rule and no recommendation reads it, so no advice changed.** **Found in passing, not fixed** (out of scope): a shipyard-queue row's **`hull` field is a misnomer** — `buildShipyardQueues` sets both `design` and `hull` to the ship *design* template name (`playerShipTemplate475`), so comparing it against a hull class silently matches nothing; and the row's **`completionDate` is systematically early by the elapsed time**, because it adds the *remaining* `daysToCompletion` to `startDate` instead of to now. **Suite 1288 / 1287 pass / 0 fail / 1 conditional skip** against a baseline of 1284 / 1283 / 0 / 1 — 4 new tests, **5 deliberate breaks each confirmed red** (queue-length divisor; unreadable status counted as not-building; unreadable horizon shrinking the delivery count; template base labelled as measured; the floor rendered as a point estimate). Verified live in the browser at 3411, **both modes**, and against `/latest-war-room.md` in both modes; no horizontal overflow at 375 px |
| **the per-faction mining scalar (tracker 3d(b)) — SOURCED, not fitted** | — | **The residual was never a mystery scalar: it is `1 + Σ(active org miningBonus) + Σ(SpaceMiningBonus effect values)`, both ADDITIVE fractions in the same units.** `shared/spaceMiningBonus.mjs` is that term. **The model now closes for everyone.** With it applied, `Σ(rate × miningModifier) × 365.25/12 × 1.15^grants × (1 + Σ) + flat module income + incoming transfers` reproduces every faction's own `cachedYearlyRevenue / 12` at **0.999978–0.999981 on all five resources for all eight factions** — 0.0022%, every digit the save carries. Across five 1.0.51 saves it closes **38 of 40** faction-reconciliations at that precision, and it does so while B *moves*: the Resistance reads 1.06 in 2029, 1.33 in 2035, 1.35 in a mid-2034 autosave, tracking its org holdings each time. **The two rivals that looked exempt were simply holding no mining orgs** — the residual table was 1.000/1.000/1.10/1.14/1.19/1.28/1.33 once two other terms were separated out (see below), and the Aliens joined the observer and the Academy at 1.000. **The observer is 1.000 in this campaign and 1.30 in `Again.gz`**, so it is not a term the observer can be assumed out of; the shipped code independently reproduces that 1.30 from named orgs on named councilors. **Three corrections fell out of the reconciliation, each a real defect.** (1) **A completed but UNPOWERED mine produces nothing**, and `constructionStatus` cannot see it — it is derived from `constructionCompleted` alone. The Servants run two of them; counting them puts their five-resource reconciliation out by 4–5% while everyone else closes at 0.002%. `habSites[].mineModulePowered` now carries the game's own flag and `resolveMineModuleMultiplier` reports `operational: false`, `poweredRead` distinguishing "checked" from "not carried". (2) **The Aliens' non-uniform residual was `AlienWormholeFacility`**, the one module with a flat resource income (+200 volatiles / +200 metals / +50 nobles / +10 fissiles per month and **no water**) — which is exactly why their water read 1.000 and their other four did not. (3) **The Protectorate's was the Aliens' standing resource transfer** (1.2/day water, volatiles and metals; 0.3 nobles). **`Effect_SpaceMiningBonus5` is granted by NO project** — the only two grants in the shipped templates are the narrative events `event_Breakthrough_Hab` and `event_ScienceTour` — and the Resistance holds it, so the effect half is read from `TIEffectsState.factionEffectsNames`, the same shape the control-point work found. **The question `shared/miningTechBonus.mjs` left open is answered**: Project Exodus reading 1.28 rather than 1.10 was never a contradiction, it holds +0.18 of org bonus *beside* `Project_GoldRush`'s +0.1, and the two additive fractions simply sum. Stacking within the bucket is **additive, measured** — two orgs at 0.06 and 0.04 give ×1.10, not ×1.1024 — where the ×1.15 project grants remain **multiplicative**. Corroborated on the wiki (`Orgs`, raw wikitext, 2026-08-22), which lists Project Exodus Engineers as granting "+10% Space Mining output bonus" and states both activity gates this module enforces: an **unassigned** org "confer[s] no benefits to the faction", and a newly bought org or one on a Detained councilor is inactive — which is what `TIOrgState.applyingBonuses` records. Values are read from `TIOrgState`, never `TIOrgTemplate`: five of the seven templates carrying `miningBonus` are `randomized`, and `RandomSpaceMining12` instances read 0.04, 0.05 and 0.06 against a template 0.04. **Rival output: modellable in omniscient, refused in player.** Every term is now readable — deposit rates and `mineModuleTemplate` are not redacted, and orgs, the effect list and the completed projects all survive omniscient — but player mode publishes only part of a rival's roster and strips `orgs` from what it does publish, so a rival resolves **`unknown`, never `measured-none`**; a confident "this rival has no mining bonus" read off a truncated roster is the worse of the two failures. `spaceMiningBonusEffects` is redacted to `null` (never `[]`) and added to the leak scan. **Measured consequence: nothing the observer sees moved.** Against a frozen `ExitSave.gz` (md5 `5c0d9ef9…`, two identical baseline runs first), 34 captured payloads across both modes changed only by ADDED fields, two restated `UNMODELLED_FACTORS` entries, one new assumptions line, and the two Servants sites flipping `mineModuleProducing` `true → false`. No `siteValue`, no `yields.*.monthly`, no briefing figure, no engine recommendation moved — the observer's bonus is a **measured** ×1.000 on this save, which is a different fact from an unmeasured one and is labelled as such. **The lower-bound label on unowned candidates does not lift**: this term is in `siteValue` because it is measured off the observer's own roster rather than decided for a site with no module, but `MINE_MODULE_PROJECTION_POLICY` still refuses the module multiplier, so a candidate's yield remains a lower bound for that reason alone. **Gap for the `shared/markdownExports.mjs` owner** (held by another agent this round, deliberately untouched): war-room §7 says the mine bonuses are the observer's completed-project multipliers, which is now only two thirds true — the figure that belongs there is the observer's faction-wide `spaceMiningBonus.multiplier` with its named sources and its unknown state, beside the existing ×1.15 line. **18 new tests, 18 deliberate mutations each confirmed red**, including two that could not pass by construction: the reconciliation asserts both that the model closes *with* the term and that it does *not* close without it. Suite **1302 / 1300 pass / 0 fail / 2 skipped**, against a worktree baseline of 1284 / 1282 / 0 / 2 (both skips environmental — no published-snapshot artifact, and the live save is not in a war-pressure state) |
| **what `history_CPCapOverageByDay` records — and the control-point cap reconciled (tracker item 7, second pass)** | — | **The previous pass refused the headroom verdict because the cap would not reconcile. It would not reconcile because the field it was checked against was read two ways wrong at once.** From the shipped assembly (`Assembly-CSharp.dll` 1.0.51, IL read directly 2026-08-22 with a purpose-built PE/CLI metadata reader): `GetOneDayControlPointCapMissionPenalty()` is `max(0, cost − cap) × TIMissionModifier_ControlPointOverage_Multiplier`, and that multiplier is `0.3333333432674408f`. So each slot holds the **mission-defence penalty, which is the overage over three** — and the array is **one slot per in-game day, NEWEST FIRST**, while the previous model read `[length − 1]` and called it "the most recent slot". That slot is 31 days stale. On `ExitSave.gz` the two errors together implied a cap of 865.48 against a composed 992; read correctly it is 841.17 against a composed 842. **Slot ordering is confirmed without the assembly** by value alignment across four MD5-verified frozen saves — offsets of exactly 16, 15 and 31 slots for gaps of 15.5, 15.5 and 31 days, in this array and in `history_MCCapOverageByDay` alike. **The Mission Control sibling settles the family's two open questions**: `history_MCCapOverageByDay[0]` equals `max(0, missionControlUsage − cachedYearlyRevenue.MissionControl)` **exactly** for all seven human factions on two saves (the Servants read 3 at 374 against 371), so it is floored not clamped; and the Aliens read 0 while 10 and 11 over, which is an **exemption** — `GetControlPointMaintenanceFreebieCap()` returns a hard-coded `20000f` for them. The repo's own `missionControlCapacity` is a nations-only sum and is not the game's capacity, which is why the earlier MC check looked hopeless. **Three consequent corrections:** the base cap is `controlPointMaintenanceFreebies` **alone** — the cap method never reads the `controlPointMaintenanceFreebieBonus` campaign knob, so the previous model double-counted **150 points on a cap of 841**; the cost divides GDP by `fixedPCGDPToRaiseBaseCPMaintenanceCostBy1` (994,239,000, frozen at campaign start as `globalGDP_CampaignStart × 6.26e-6`), not by 1e9; and only `benefitsDisabled` makes a control point free — `SetCrackdownExpiry` writes no such flag, so the wiki's crackdown exemption is **not** in the 1.0.51 code path and crackdown-only holdings are charged and counted under `crackdownChargedCount` rather than forgiven. **It now reconciles**: cap implied by `cost − 3 × penalty` against the composed cap is 838.99978 vs 840, 841.11872 vs 842, 841.23206 vs 842, 841.16660 vs 842 across four saves — an integer to four decimals on the first, so the cost formula is exact and the whole residual is **one point of councilor-attribute resolution that does not grow across six in-game months**. It is reported in `CONTROL_POINT_CAP_ACCURACY` and deliberately **not** subtracted out. Corroborated one-sidedly across 6 saves × 7 factions with no violation, tightest margin 9.99 (which the 150-point double-count would have shown as 159.99). **`headroom.available` is now true**, from one of two named bases — `recorded` (exact, when the save records a positive penalty; no composed cap involved) or `composed` — and still refuses on an unreadable term, a floor-only cost, an unmeasured normalizer, or a **contradiction** (a negative composed headroom while the game records zero). The observer has **237.70** of room against a cap of 696; the Protectorate is **34.34 over**, paying **1,179 Influence/yr**, with **+10.64** to hostile Crackdown/Purge/Enthrall/Dominate — the 32-day mean the game actually applies, not today's 11.45. **Both modes**: identical for the observer, every rival's cap, overage, slot 0 **and** window mean `null` in player mode, all three in `assertPlayerSnapshotSafe` individually, and the whole payload scanned for their omniscient values (the overage is slot 0 × 3, so redacting only the derived field would leak the same fact twice over). **Still an annotation, not a rule** — the reason changed from "no honest threshold" to "the threshold is not close": 238 of room against a dearest control point of 39. **No recommendation changed**, captured before/after against a frozen `ExitSave.gz` in both modes with two identical baseline runs proving the harness deterministic — the only differing lines in the entire briefing are the annotation's rounding (3 → 5 places, same values) and its formula label. 13 new tests; **11 deliberate mutations each confirmed red**, including re-reading the oldest slot and dropping the ×3. **`shared/markdownExports.mjs` was out of scope** (held by a concurrent agent): the observer's headroom and the recording's semantics are not yet in the AI exports |
| **six repo-hygiene follow-ups** | — | **Five closed by changing something, one closed by measuring that its premise was wrong.** **(1) `res.sendFile` and the dot-directory 404 — the one that was costing real time.** `send` 1.2.0 defaults `dotfiles` to `'ignore'`, and with no `root` option it tests EVERY segment of the absolute path, so `/` and `/v2` 404'd on any checkout under `.claude/worktrees/`. The API routes and `express.static` were unaffected because both set `root`, which is why it presented as a blank dashboard and was nearly diagnosed as a CSS regression. Fixed by passing `{ root: publicDir }` and a relative `'v2/index.html'`, not by `dotfiles: 'allow'`: **on the code as written the two are equivalent** — the route serves one constant path and reads nothing from the request, so neither widens what it can reach — but `'allow'` switches the guard off, and would silently serve `.env` or `.git/config` if a request parameter ever reached that path. `express.static` is untouched and still 404s a dotfile. The handler moved to `server/http/routes/shell.js` for ONE reason: an injectable `publicDir` lets a test reproduce the bug on any checkout, where a plain `GET /` could only ever fail on a machine that already had it. **`tests/cssComputedStyle.test.js` passes in a worktree for the first time.** **(2) The five `paths.*SubDir` keys stay, and the follow-up's premise was wrong.** "A schema that requires a key nothing reads will reject a valid config" — it cannot: `server/config.js` and `Get-TIConfig` both validate AFTER merging `config/defaults.json` underneath, so the `required` entry is unreachable for any user config. Measured across five shapes (absent file, `{}`, nested without `paths`, nested `paths` without sub-directories, legacy flat) — all five resolve. They are also read, by seven PowerShell scripts and `export_factions.py` through the same `config/config.schema.json`. Removing `csvSubDir` from `defaults.json` turns four tests red with `/paths: must have required property 'csvSubDir'`, which is the mechanism itself. **(3) `export_factions.py`** now falls back to `config/defaults.json` when the gitignored `config.json` is absent, reads the nested `paths.*` spelling before the legacy flat one (a nested config used to give it the WRONG output directory rather than an error), resolves a relative `workDir` against its own folder like its PowerShell siblings, and refuses an unconfigured save path instead of interpolating the literal string `'None'` into a PowerShell command. Behaviour on the actual `config.json` is unchanged — it is flat and absolute. **(4) The four root `test_browser*.js` scripts are deleted.** Nothing referenced them; three wrote screenshots into one 2026-08-19 agent session's artifact directory; `test_browser.js` asserted on v1 `.card` markup at the site root. The two citing archive documents carry a dated note, and `repo-structure-spec.md` records the resolution. **(5) `generate_hab_module_tables.ps1.bak` is deleted, and it did differ** — 116 lines against the live script's 234. The live script is a strict superset of its DATA (it emits `miningModifier` and 30-odd more columns, plus upkeep and build-material tables); the only thing lost is a `Get-HabModuleCategory` heuristic that grouped modules into Core/Mining/Shipyard/Research/Administration/Defense/Resupply/Utility, and every field that heuristic read is still emitted, so the grouping is reconstructable. **(6) The two hand-copies of `escapeHtml`** in `tests/driveExplorer.test.js` and `tests/drivePathModal.test.js` now take `MISSION_CONTROL_SHARED` from `tests/fixtures/renderHarness.js`, which executes the shipped `public/v2/js/shared.js`. Not a bug fix — both copies were faithful — but renaming the export in `shared.js` now turns both files red, which it did not before. **Nothing user-visible moved**: 114 requests over 113 distinct payloads, captured through the live Express app against a frozen `ExitSave.gz` (md5 `5c0d9ef9…`) in a three-save folder with pinned mtimes — all 38 intel resources, the whole snapshot, the compact snapshot, briefing, export, the three `.md` exports, six tech routes and the discovery index in both HTML and JSON, in BOTH modes, plus the four runtime routes. **111 of 113 byte-identical; the two that moved are `/` and `/v2`, 404 → 200**, which is the point of the change. Both now serve bytes identical to `/v2/index.html`. Two identical baseline runs proved the harness deterministic before any of it was trusted. **Suite 1252 → 1259 tests.** In a bare worktree, **1249 → 1257 passing, 1 fail → 0, 2 skips unchanged**; after `npm run build:site`, **1258 passing / 0 fail / 1 skip**, which is the main checkout's shape (its baseline was 1252 / 1251 / 0 / 1). The seven new tests are five in `tests/missionControlShell.test.js` and one each in `tests/repoStructure.test.js` and `tests/config.test.js`; the eighth extra pass is `cssComputedStyle`, which had been the failure. The two conditional skips are unrelated to any of this (`dist/` absent until the site is built; the live save not in a war-pressure state), and the worktree baseline reconciles with the main one exactly: worktree = main − 1 (the 404) − 1 (`dist/`). **Eight deliberate mutations, each confirmed red**, including a control that must stay GREEN: the same registration serves fine from a non-dot directory, so the dot segment is the variable and not a broken fixture. Renaming `escapeHtml` in `public/v2/js/shared.js` now turns `driveExplorer` and `drivePathModal` red, which is the proof the copies are actually gone rather than merely deleted from view |
| **the strategic commentary reaches the AI exports, and two null-collapses under it** | — | **The byte-budget decision was the task, and it is a decision to carry a SUBSET.** The whole commentary payload serialises to ~10 KB against ~6.95 KB of war-room headroom, and most of that is waste: the five per-tier `uncertainty` records are ~1.8 KB **each** and identical by construction (one sweep supplies one `opponentRatingBasis`; the seed/trial/sweep figures are module constants), so they collapse into **one** statement. Also dropped: the `headline`, which a seeded PRNG picks from three interchangeable strings for the same beat set — two saves with identical beats print different headlines, so an agent diffing them would read noise as signal — and the `prose` (630 bytes), a narrative restatement of the beats, the two named tiers and the advice, all of which are carried structurally. Both are at `/api/v2/briefing`, which the section's last line names, the same relationship §9 and §10 have to their endpoints. **Carried:** the engine's own recommendation (the §10 lesson — a section of counts renders byte-identical across a change that moves the advice), the beats with severity/stance/id, the own-side rating every hull count is denominated in, all five tiers with the measured armour/CV under each, both projections, and the **calibration warning**, which is the one part that may never be separated from the numbers. The one per-tier field that genuinely varies, `winnableRatio`, is carried **only when below 1** — exactly when the percentile band was taken over a subset of seeds and understates its own spread. **Destination: war room §11, not threats.** Threats has 2,676 bytes of headroom and its §3 already runs the *same* engagement model per fleet; a second modelled-combat block there would duplicate register and burn the remaining budget. **Degradation:** §11 is a fixed block with no ladder entry, because it is fixed-size by construction — the engine defines exactly five beats and both opponent-tier builders return exactly five tiers — and it is **first in `clampOrder`**, before §9 and §10, because it is the only body in the document with no measured content of its own: every input it reasons over is already printed in §1–§5. Measured: at a 6,000-byte cap it is the **only** body suppressed; at 4,500 it is joined by §9 and §10 and nothing measured has moved. It degrades whole rather than row by row because a table of hull counts with the caveat cut away reads as measurement. **Byte cost, live save (`ExitSave.gz`, 1/1/2035), against 30,720:** war room **23,754 → 26,195** omniscient (§11 = 2,440, headroom 4,525) and **23,779 → 26,377** player (§11 = 2,597, headroom 4,343); with no runtime to hand the commentary over it is 419 / 415 bytes of explicit unavailability instead. `/latest-threats.md` (7,562 / 7,564) and `/latest-snapshot.md` are **byte-unchanged**. **Re-measured under growth with a full §10 AND a full §11 at its 4,105-byte worst case** (all five beats, all five tiers, a partial `winnableRatio` on every row, both projections): under the cap at every level to **40× / 40×** in both modes, peak 30,378, and §11's size is **identical at 1×, 5× and 20×** — and the friendly/hostile row counts are the same with and without it at every level, so it displaces no fleet entry. Like the cycle plan it is engine output, so Express hands it in off the **same single briefing generation** that already produces `cyclePlan` and `primary`, and the hosted worker needs no change: `strategicCommentary` is a sibling of `engineDirectives` on the `missionControlBriefing` every published row carries. **Two null-collapses underneath it, both fixed.** (1) `rebuildClock`'s `Math.max(1, Math.round(...))` — `Math.round` takes every rate under 0.5 to 0 and the floor replaces that 0 with a 1, so a hull buildable once every four months published as "~1 hulls/mo". Live save, **both modes: 1 → 1.25**, plus a new `daysPerHullEst` of 24, because below one a month the usable sentence is the reciprocal. Rounding is to three **significant** figures, not two decimals: a 4,000-day hull runs at 0.0075/mo and `round(x, 2)` prints that as a confident 0. (2) `facts.js` flattened an absent `shipyardQueues` to `[]`, so `simulation.js`'s `queuedCount === null` refusal **could never fire** — a queue nobody read was published as a faction building nothing. It now stays null, and deliberately does **not** fall through to `rawSnapshot`, because queues are filtered intelligence rather than static reference data. **A third, found while making §11 honest and arguably the worst of them: `hateVent` collapsed four states into one bare `null`, and one of them is player mode's redaction of `actualAlienHate`** — so player mode could not produce a vent horizon under ANY campaign state and said so with the same silence it uses for "hostility is below the floor", an unmeasurable input reported as a reassuring finding. Verified live in the browser at 3117, both modes, with genuinely different reasons: player "redacted outright… the ONLY outcome player mode can reach", omniscient "no hate trend could be measured". The COMMAND card now reads `1.25 hulls/mo — 1 per 24 days (5 ship(s) queued, assumed parallel)` where it read `~1 hulls/mo … (5 active yards)` — "active yards" was wrong, and the parallelism it assumes is now stated on both surfaces and left as an open item. **Suite 1277 / 1274 pass / 1 fail / 2 skipped** against a worktree baseline of 1252 / 1249 / 1 / 2 — the failure is the same `tests/cssComputedStyle.test.js` dot-directory 404, red before the change too. 25 new tests, **6 deliberate mutations each confirmed red** — the floor, the queue flattening, the vent collapse, the calibration warning, §11's `clampOrder` position, and dropping the handover from the Express route (which the renderer's own tests cannot see, so the route test pins it as it already pins `primary`). Adding §11 also exposed a live harness defect: `sectionTen` sliced to **end-of-document**, so §11's own "UNAVAILABLE in this runtime" line was being read as §10's — a false failure this time, but the same over-broad extractor would as easily have produced a false PASS. Every numbered-section extractor is now bounded by the next heading |
| **the mine module's own output multiplier (tracker 3d(a), follow-up to `a615018`)** | — | **The larger of the two missing mine terms is closed for owned sites and deliberately REFUSED for unowned ones, and the refusal is the substance of the change.** `TIHabModuleTemplate.json` (read 2026-08-22) carries exactly seven modules with `mine: true`; `shared/mineModuleOutput.mjs` is that table plus the discipline around it. **The join is by template name, never by tier** — `AlienSettlementMiningComplex` is ×2.0 at the same tier the human `SettlementMiningComplex` is ×1.5, so a tier-keyed model would understate an alien mine by 25%. **A non-operational mine produces nothing, measured on three factions**: folding `building` modules into the income model turns Project Exodus's five-resource reconciliation spread from **1.4e-5 into 1.2e+0**, the Resistance's from 1.2e-4 into 8.0e-1 and Humanity First's from 1.4e-5 into 3.0e-1. **The model now closes.** With the module term and the ×1.15 tech term both applied, the observer's advisable-hab output reconciles against the game's own `financials.projectedMonthlyIncome` at **0.999962–1.000018 on all five resources** (max error 0.004%), so its owned figures are no longer a lower bound. The briefing sentence moves **Water 26.1 → 27.5, Volatiles 31.8 → 33.5, Metals 35.4 → 41.9, NobleMetals 8.9 → 10.5, Fissiles 0.2 → 0.3 per day**, identically in both modes. **`monthlyIncome` is untouched** — it is the measured 30-day ledger and a second application would be the Engineers +95% error. **THE REFUSAL.** An unowned candidate has no mine module (272 of 272 scored candidates carry none), so projecting its yield means DECIDING a tier. The expansion score saturates, so a uniform assumed multiplier **reorders the board rather than scaling it**: 60 of 85 candidates change rank between ×1.00 and ×1.25, **64 of 85 between ×1.25 and ×1.50**, and 54 of 85 between ×1.50 and ×2.00 — while the **top nine are identical under every one of those multipliers**. A projection therefore buys no change in the advice and costs 54–65 places of assumption-driven churn, so it is not applied and `MINE_MODULE_PROJECTION_POLICY` records the decision, the evidence and the three rejected rules (highest buildable contradicts what the observer actually builds — 16 of its 17 mines are cheaper tiers; modal tier makes the ranking of unowned sites depend on past building habits; "cheapest" is ambiguous because the ×1.25 Automated complex is cheaper than the ×1.0 Outpost on crew, power, build time and upkeep). **Measured consequence: the expansion board's ordering did not move at all** — 85 candidates, `siteValue`, `yields` and `valuePerHate` byte-identical in both modes, against `a615018`'s 66 of 85. Each row instead carries a **band** (`×1–×1.5 once mined`) in the estimate register, never a point — and the band lives **once** on `mineModuleCapability` with each row pointing at it, because duplicating it plus a 330-character caveat across the 357 emitted rows grew this endpoint by 23%; trimmed, the growth is **599,093 → 661,482 bytes (+10.4%)**. **What DOES change the advice is measured, not projected: the mine-upgrade board.** The observer has **headroom 1** (17 mines against a limit of 18) and its water runway is **critical at 2.1 months**. Upgrading its **10 Outpost complexes** to Settlement is worth **+308.8 water / +378.7 volatiles / +185.3 metals / +53.9 nobles per month** and costs **0** against the mine limit (every complex is `onePerHab` and the successor names the module it replaces), where the best available new claim is worth +86.8 water and consumes the last headroom. **Nothing upgrades from `AutomatedMiningComplex`** — its six sites are reported `no-upgrade-path`, not folded into the gain, and the one Settlement site reads `not-researched` naming `Project_ColonyMiningComplex`. **Three states, never a fall-through**: `measured`, `not-built` (multiplier `null`, contributes nothing — a ×1.0 would claim an Outpost that does not exist) and `unknown` (an unrecognised module keeps its raw rate and is counted, so the total is honest-low). A fourth, whole-snapshot case was added after the existing suite caught it: a snapshot that never carried `mineModuleTemplate` would otherwise read as "no site has a mine" and zero a faction's entire output, so `mineModuleDataAvailable` degrades the whole set to RAW rates **and says so**. **Both modes verified and identical** — every term is the observer's own; `habSites[].mineModuleTemplate` is *not* redacted (129 modules visible in both modes, unlike `habModules`, which is 107 vs 2,099), so a leak here would not have shown up as a mode difference, and `the upgrade board only ever lists the OBSERVER's own sites` pins it directly. **`shared/markdownExports.mjs` is untouched and its war-room §7 wording stays TRUE**: §7 points the multiplier at `/api/intel/mining` and `/api/intel/mining-expansion`, and neither endpoint's deposit rates or candidate yields moved — `mining` and `body-status` gained three additive fields (`mineModuleMultiplier`, `…State`, `…Producing`) with **zero** values changed. **76 of 82 captured payloads byte-identical**, against a frozen `ExitSave.gz` (md5 `5c0d9ef9…`) with two identical baseline runs first. The engine's *committed* cycle plan, `primary` and `rejected` are byte-identical in both modes; what moved is the valuation and ordering of the rejected Advise-hab alternatives, the same surface `a615018` moved. **Suite 1225 tests / 1222 pass / 1 fail / 2 skipped**, against a worktree baseline of 1196 / 1193 / 1 / 2 — the one failure is `tests/cssComputedStyle.test.js`, which fails identically before the change because `res.sendFile` 404s a path under a dot-directory, and the two skips are the same conditional ones. 29 new tests, **29 deliberate mutations each confirmed red** — three started GREEN and exposed real gaps: the upgrade board's observer scoping was unpinned (pointing it at faction 4713 passed every test), the projection-policy evidence could lose its counts unnoticed, and an unreadable rate could become a confident zero in the upgrade total. A browser check (`scripts/verify_mining_registers.js`, 41 assertions across both modes) reads the two registers off the live DOM with `getComputedStyle`, and a cascade-only break that source inspection passes was confirmed to fail it — which is also why the in-suite guard now asserts declaration ORDER, not just that the two rules differ |
| **a cluster of confident defaults and tests that could not fail** | — | **Five defects of one shape, each proven by a deliberate mutation rather than by a passing suite. (1) `tests/powershell_common.test.js` reported six green passes having executed NO PowerShell** — every test spawned `pwsh` and did a bare `return` on ENOENT, which node:test scores as a pass, and `pwsh` is not on this machine while Windows PowerShell 5.1 is. All six ran in ~2.5 ms. They now resolve a host (`pwsh` → `powershell.exe` → `powershell`), require a **sentinel on stdout** so a probe that never reached its last line cannot pass, and with no host at all declare `{ skip: <reason> }` naming every candidate — verified as **8 skipped / 0 passed** with PowerShell stripped from PATH. Each of the six was confirmed red by a mutation of `TerraInvicta.Common.psm1` (sort order, nested merge, the open effects map, legacy migration, the schema maximum, the retention alias), and the file carries a self-test that runs a throwing script and asserts it scores red. Probes now take 578–1066 ms under 5.1.19041.6456. **Running them for real immediately found something the vacuous version could not**: `Get-TIConfig` applies `TI_SAVE_PATH` / `TI_TEMPLATES_DIR` / `SUPABASE_*` **after** the merge, so a shell with any of them set made the nested-merge probe assert on the environment and fail. Every probe now clears those five variables first, and a seventh test pins the override behaviour deliberately in both directions — file wins with a clean environment, environment wins when set, including through the legacy `SavePath` projection — with its own mutation confirmed red. **(2) `toFiniteNumber([]) === 0`** — the three-way absence guard was followed by a bare `Number()`, so an array became a confident **0**, `[7]` became 7, `true` became 1, `false` became 0 and `'  '` became 0. **Measured before changing anything**: the divergent branch was instrumented and fired **0 times** across the whole suite and across **132 captured surfaces** (38 intel routes, the filtered snapshot, the briefing and all four markdown exports, in all three modes). No caller relied on it, so the rule is tightened rather than 598 call sites migrated, and `strictFiniteNumber` becomes an **alias of the same function object** rather than a second implementation. All 132 surfaces byte-identical, two identical baseline runs taken first. **(3) Two silent harness defects** — a sandbox `escapeHtml` that did not escape (`{ window: {} }` left components on their non-escaping fallback; other harnesses passed `s => s`) and a `visibleText` that stripped tags without decoding entities, so `&lt;1 mo` either read as the entity or was eaten as a tag and a row's duration vanished from what the test believed a reader sees. `tests/fixtures/renderHarness.js` executes the **shipped** `public/v2/js/shared.js` for the real escaper and decodes entities; five harnesses use it, including a second one in `researchRanking` that the earlier partial fix had missed. Proven by rendering the same payload through the old broken sandbox and asserting it **loses** the duration. **(4) Seven latent defaults in `server/commentary/simulation.js`** — `armorMedian \|\| 10`, `\|\| 12` / `\|\| 24` armour percentiles, p10/p50/p90 CVs of 3250 / 20330 / 70100, `baseConstructionTimeDays \|\| 60`, `queuedCount \|\| 2`. Measured latent on `ExitSave.gz` (0 of 57 alien fleets miss `armorMedian`, 82 of 82 alien designs carry a positive CV, 0 of 28 hulls miss a build time), and latent is not fixed. Unreadable inputs now refuse and say which measurement was missing, an **empty** queue is a measured zero rather than two invented yards, and "no alien forces visible" is no longer reported for forces that are visible but unmeasurable. The three CV fallbacks turned out **unreachable, not latent** — restoring them leaves the suite green — so their removal is recorded as dead-code removal, not a behaviour change. The whole `strategicCommentary` payload is **byte-identical in all three modes** after the change, which is the evidence they were latent. **(5) `fleetSizes` computed and never used**, the only place `shipsCount` was read there; removed, with a test asserting a 40-ship and a 1-ship fleet of the same armour rate identically. Six of seven mutations confirmed red |
| **war-room §10 names the recommendation, not just its counts** | — | **`b5ca8dd` changed the omniscient primary from "Advise Government: USA" to "Purge the Protectorate hold on ExtractiveSector in China" and took `totalExpectedValue` from 21.41 to 66.13 — and all three markdown exports rendered BYTE-IDENTICAL across it, in both modes.** §10 printed only counts (risk floor, bench slice, "5 councilor(s) assigned") and none of them move when the recommendation does, so the largest user-visible change the engine has produced was invisible to every LLM reading the exports. One summary line now carries it: `Primary: <title> — score X, EV Y \| whole-cycle totalExpectedValue Z`. `primary` is a **sibling of `cyclePlan`** on `engineDirectives`, so the hosted worker's existing fallback finds it with no worker change, and the Express route hands both over off a **single** briefing generation. **Measured, both modes, frozen `ExitSave.gz`:** omniscient "Purge the Protectorate hold on ExtractiveSector in China — score 68.75, EV 45.93 \| 66.13"; player "Advise Government: United States of North America — score 7.00, EV 10.03 \| 19.30" — genuinely two plans, not a redaction of one. **Byte cost: player 23,650 → 23,779 (+129), omniscient 23,617 → 23,754 (+137), against the 30,720 budget — 6,941 / 6,966 bytes of headroom, nothing dropped.** The EV comes from `primary.assignment.expectedValue`, not from `primary`: the candidate carries the score, the expected value exists only once the action is paired with a councilor, so an unpaired primary reads `EV UNAVAILABLE` rather than 0.00. Three absent states are distinct and pinned — no plan read at all prints no primary line (§10 already says nothing was read), a plan with no primary says so, and an unmeasured score/EV/total renders UNAVAILABLE. Across **138 captured surfaces** in three modes the ONLY delta is this one line. Four deliberate mutations confirmed red, including one that drops `primary` from the Express route and one that coerces the unmeasured figures to 0.00 |
| **the total-war gate on `/api/intel/alien-threat` (follow-up to `85c8f8a`)** | — | **The one endpoint whose entire job is the alien threat was the one surface the verdict never reached.** `/api/snapshot`, `/api/v2/briefing` and `/latest-war-room.md` all carried the gate; the endpoint carried `actualHate`, `usedMC`, `difficultyMultiplier` and the projections those figures are *for*, and stopped. **The recorded cause was verified before anything was changed**, not assumed: `shared/intel/alienThreat.mjs` built its hate economics with no `yearsElapsed` and no `alienProgressionSpeed`, so `buildTotalWarState` answered `state: 'unavailable'` in **all three modes** with a 20-year Normal gate from an assumed 1× speed. It never published a *wrong* verdict — that function refuses a false `safe` when elapsed years are missing — it published none, and the resource never emitted the block at all. **Fixed by forwarding the two missing inputs through `shared/campaignElapsed.mjs`**, which exists so two callers cannot disagree; `buildTotalWarState` remains the single computation and this module only hands it what it needs. The endpoint now emits `totalWar` **byte-identical** to `alienHateEconomics.totalWar` on the snapshot for the same save and mode, asserted by `deepStrictEqual` through the real `applyFilter` in every mode, plus `totalWarStatus` (`available` \| `unavailable`, never a degraded `safe`), `totalWarSource`, `campaignAgeBasis` and `daysInCampaign`. **Measured on `ExitSave.gz` (md5 `5c0d9ef9…`), omniscient / player:** state `safe` / `safe_hate_unknown`, year gate **10** (Normal 20 ÷ the save's 2.00× Alien Progression Speed), elapsed **8.91** (`daysInCampaign` 3256 ÷ 365.25), remaining **1.09**, `maximumAlienHate` **2782**, `hateRemaining` 157.13747 / **null**. A whole-payload scan of the player response for the raw hate (42.86253) in four renderings found **no leak**. **A third hand-copy of the campaign-year regex was about to appear**, so `resolveCampaignYear` moved into `campaignElapsed.mjs` and `server/intelligenceFilter.js` and `shared/strategicSnapshot.mjs` now read it too — proved inert by capture-and-diff. **The v2 THREAT view is unaffected**: it renders the gate from `state.rawSnapshot.alienHateEconomics` via `/api/snapshot` and never fetches this endpoint, so nothing in the browser changed. **`/latest-threats.md` gained the gate as one header line, and that was a judgement call**: the war room already carries the derivation, but this document reports contacts inbound within **365 days** while the year gate is 1.09 years — about **398 days** — away, so its reader is planning inside the window the gate opens in. **Byte cost 178 / 159**: 7,386 → **7,564** (player) and 7,403 → **7,562** (omniscient) against 10,240, leaving 2,676 / 2,678 of headroom. It sits in the non-degradable title block on purpose — a strategic clock should not be what the budget ladder sheds — and an absent gate renders `UNAVAILABLE`, never `SAFE`, which the committed pre-gate fixtures pin. **Proof of no change**: snapshot economics, `/api/intel/alien`, war-room, snapshot and threats markdown captured in all three modes against a frozen save, `diff -rq` to **exactly the three `alien-threat` payloads**, then re-captured after the mutation passes and confirmed identical. **13 new tests, 7 deliberate mutations each confirmed red** — including `yearsElapsed ?? 0`, which flips the honest `unavailable` to a confident **`safe`**, and `Number(null).toFixed(1)`, which renders a redacted hate distance as **`0.0`**. One assertion was wrong on first writing and the failure was informative: when the gate is unavailable `buildTotalWarState` withholds `hateRemaining` too, even though hate is individually known — it does not publish a half-evaluated gate |
| **the control-point cap composition (tracker item 7)** | — | **SUPERSEDED 2026-08-22 by the row at the top of this table: the refusal below was right about its evidence and wrong about the evidence's meaning, and the base cap of 550 double-counted a campaign knob.** **Shipped as an explicitly-modelled figure with a refusal, and three of the spec's own claims were wrong.** (1) The spec named **Persuasion only**; the wiki `Control Point Capacity` (raw wikitext, 2026-08-22) says **Administration, Persuasion *or* Command** on any non-detained councilor, and adds that orgs and traits contribute nothing directly — so the resolved attribute is the right reading and there is no separate org term. (2) The spec said **"no per-faction cap field was found"** and pointed at `TIGlobalConfig.json` (a 4.7 KB list of UI canvas names) and `TIFactionTemplate.json` (no cap field). The base cap **is** in the save, in two fields, and the wiki `Game_Options` names both knobs — "Base Control Point Capacity" and "AI bonus Control Point Cap". (3) The spec missed **hab modules entirely**: `AdministrationNode`/`Tower`/`Complex` carry `controlPointCapacity` 4/12/30, 3 of 156 modules. **The sign convention is pinned twice by the shipped data**, not assumed: all five `ControlPointMaintenance` effects carry `"showTotal": "Invert"`, and `AI_projectRole: "ControlPointCap"` is shared between the nine projects granting the **negative** effects and the three that unlock the **positive**-capacity modules — so −120 raises the cap by 120. Effects are read from `TIEffectsState.factionEffectsNames`, not from completed projects, because **the Aliens hold four such effects granted by none of the 32 projects that grant them** (two list `initialFactionsStr: ["AlienCouncil", …]`), which a project sweep scores at 0 against a true 320. **Four separate refusals, each with a deliberately-broken test proving it fires:** no base → whole cap unknown; a short roster → unknown (player mode publishes **0 of the Aliens' 6** councilors and 4 of the Protectorate's 6, so summing the visible rows would delete the largest term); a faction with habs but no visible module rows → unknown (the Servants: 50 habs / **0** modules in player mode, 50 / 574 in omniscient); an absent effect list → unknown while an empty one is a measured zero. **Both modes**: the observer's own cap composes identically (846, same per-councilor breakdown) and every rival's refuses in player mode on all three grounds, with their recorded overage and effect list redacted to `null`, added to `assertPlayerSnapshotSafe`, and the whole player payload scanned end-to-end for a rival's value rather than one field pinned. `/api/intel/control-point-cap` is one registry row, **appended** so no existing example is renumbered. **The engine got an annotation, not a rule**, and **no recommendation changed** — the cycle plan is identical before and after in both modes (primary "Advise Government: United States of North America" at 6.997422015983501, five omniscient / three player assignments, totalExpectedValue 21.41 / 19.3, Madagascar still benched at 5.63). A veto would have to test against a cap that does not reconcile; the *marginal* per-control-point cost does not depend on the base at all, so it ships as data (Madagascar's Executive ~3 cp against ~39 for one of the USA's six). **`shared/markdownExports.mjs` was out of scope this round** — the figure that belongs there is the observer's composed cap with its refusal, and it is not yet in the AI exports. 29 new tests, **14 deliberate mutations each confirmed red** |
| **`research-category-rate-spec.md` (tracker item 3) — and a verdict in `campaign-settings-spec.md` overturned** | — | **Two independent defects, found while fixing one.** (1) **The rate.** `monthsAtIncome` divided a remaining cost by the WHOLE faction's research income, but a project runs in one slot and receives only that slot's share — measured at 0.4658×, 0.2928×, 1.0602× and 0.2928× on the observer's four pipped slots, so the printed figure was 2.15×–3.42× too SHORT on three of them. `shared/researchAllocationPricing.mjs` prices it through the slot instead, in four labelled states: `allocation-measured` (pip weight read from the save), `allocation-assumed` (`one-pip` headline with `all-pips` beside it — a **7.01×** spread that a single number would have hidden), `slot-receives-nothing` (**null** months, never zero), `flat-rate-unpriced` (no layout readable; the old figure, labelled). The model reproduces all four measured slot deliveries with **zero free parameters** to a single common factor of **0.9858**, so a priced duration is stated as good to ~1.5%, not to the digit. (2) **The cost, which is the larger error and was a recorded verdict.** `campaign-settings-spec.md` cleared `researchSpeedMultiplier` on 2026-08-21 as acting "on output, not on cost". **It acts on cost**, and every research cost, completion percentage and duration was 2× too high. Three independent lines, 2026-08-22: the observer's own 10,000-point Gas Core Fission Reactor VI completed from **4,708.568** accumulated inside 15.5 days at a **measured** 30.2467 points/day, which reaches 5,000 in 9.64 days and cannot reach 10,000 in under 175; **278 in-progress rows** across the five saves at 200% never exceed 50% of template cost (max **0.49716**) while saves with no readable multiplier reach **0.9756**; `useHarshTree` is false throughout. The superseded evidence (Fleet Logistics 44,780/45,000) came from **`First.gz`, which carries no custom-difficulty block at all** — it proved that a campaign *without* a multiplier charges template cost. The 2.11× income measurement is correct and **cannot discriminate**: both sides are research points, so "income doubled, cost unscaled" and "income unscaled, cost halved" predict the same figure. **897 tech-graph nodes rescaled, 2 `researchCost: -1` sentinels correctly untouched, 0 nulls**; `Project_PherocyteMastery` reads 45.9% where it read 22.9%. **Movement, both modes, against a frozen MD5-verified `ExitSave.gz` with two identical baseline runs first**: 34 of 100 captured surfaces changed, 66 byte-identical. Durations moved on 2,153 of 3,012 rows across the four modules — drive-explorer ×1.33–2.00 (median 1.57), propulsion ×1.56–1.60, military-value ×1.21–2.00, economic-value ×0.33–3.27 (the widest, because it mixes global techs, projects and three items in measured slots). **No recommendation order changed anywhere** — all 14 ranked lists identical in both modes, and the reachability census held at 122 within / 14 beyond / 3 unknown, though the one beyond-horizon chain went from 3.87× over the horizon to **1.12×** over. Chains are repriced at **full concentration** and renamed `monthsAtFullConcentration` so no consumer reads a new number under the old meaning. Payload ceiling raised 280→300 KB, deliberately, with three further fields **refused** (measured +52.7 KB for information already recoverable). 24 new tests, **9 deliberate mutations each confirmed red**, and two test-harness defects found on the way: `visibleText` did not decode entities and the sandbox's `escapeHtml` did not escape, so a `<1 mo` duration silently vanished from "what a reader sees" |
| **`repo-structure-spec.md` (tracker item 5)** | — | **Shipped, and three of the spec's own measurements were wrong on re-checking.** The 2025-11-22 report tool — 12 scripts and docs, plus `Ship_Info/`, `csv/` and `Again_Save/` — moved into `md-generation-reports/` with `git mv`, and the three 2026-08-19 feedback documents into `docs/archive/`. Root tracked files **37 → 22**; top-level entries **53 → 36**; 44 files now under `md-generation-reports/`. **(1) `screenshots/` is not the tool's.** The spec listed its 31 tracked files as tool output; `git log --diff-filter=A` dates the directory **2026-08-19**, and it holds `qa-mission-control-map.png` and `v2-review/01-player-desktop.png` — dashboard QA, cited by `docs/archive/opencode-feedback-8-18-2026.md`. It stayed. **(2) The tool is not self-contained, and cannot be.** The spec's acceptance asked that nothing inside `md-generation-reports/` reference the dashboard, measuring only *inbound* references. Measured *outbound*: 7 of the 12 scripts `Import-Module` **`TerraInvicta.Common.psm1`**, which is dated **2026-08-20**, is also imported by the five root `parse_*.ps1` parsers, and is covered by `tests/powershell_common.test.js` — it is shared repository infrastructure the 2025 scripts were retrofitted onto, not part of the tool. `Get-TIConfig -BasePath` likewise resolves `config/defaults.json`, `config/config.schema.json` and the ignored root `config.json`. Each moved script therefore anchors the module, the config and the save-folder lookup on `$repoRoot` (its parent) while its own output root follows it down, so `csv/` / `Ship_Info/` / `Again_Save/` stay beside the scripts that write them and `config/defaults.json` needed no edit. **(3) Two of the spec's four constraints were already satisfied**: `backups/`, `logs/`, `Again` and `*.gz` were all already in `.gitignore`, and `CLAUDE.md` cites no path that moved. What `.gitignore` *did* need was the anchored generated-output patterns re-pointed — `csv/*.csv` at the root would have started tracking every generated CSV. **Proof of no change**: 126 surfaces captured through the live Express app against a frozen `ExitSave.gz` (md5 `5c0d9ef9…`) in a frozen three-save folder — whole snapshot, briefing, export, all 37 intel resources, the 7 tech routes, all three `.md` exports, the discovery index and the served shell — in **both modes**, all 200, **byte-identical before and after**. Two identical baseline runs proved the harness first and caught its one nondeterminism (`/api/runtime` mints a random `publishToken` per process). Token counts over `server shared site public scripts tests`: `?? 0` 162, `\|\| 0` 252, `=== null` 724, `sameId(` 192, `Available` 193 — **unchanged**. `Measured` moved 172 → **173**, the one occurrence being the word starting a sentence in a comment in the new test file (`tests/repoStructure.test.js:27`); there is no `*Measured` identifier in it, and the line-based grep cannot tell prose from code. Lines moved 88,521 → 88,754 and files 245 → 246, both accounted for exactly by that 233-line file. `docs/code-index.md` regenerated **byte-identical** (181 modules): the move touched no module the index scans. Suite **1134 → 1141**, the whole delta being the seven checks in the new `tests/repoStructure.test.js`; fail and skip counts are unchanged at **1 fail / 2 skip** before and after, the failure being the pre-existing dot-directory `res.sendFile` defect and the extra skip being `dist/` absent in a worktree (`npm run build:site` flips it to a pass, measured). That guard pins the one dependency that crosses the boundary and the module-resolution spelling that would break all seven scripts at once. **Its first cut was wrong and is worth recording**: it asserted `fs.existsSync(oldPath) === false`, which is not the property a move guarantees. It passed in a fresh worktree and **failed in the main checkout**, where `csv/` holds 45 untracked generated CSVs and `Again_Save/` two stray summaries that `.gitignore` has always covered — no defect behind it, and the only way to green it was to delete the user's untracked local data. Retargeted to `git ls-files`: **no *tracked* file remains under the old root paths**, and each output directory has tracked content under `md-generation-reports/`. Re-verified by reproducing that exact litter (45 untracked CSVs, 2 stray summaries) and running the whole mutation set against it: **10 deliberate breakages each confirmed red, and one control — the litter itself — confirmed green**, restored green. A `trackedUnder` probe that returns nothing now turns **five** tests red rather than greening the file, so a git that cannot answer fails loudly. `build:site` and `push:dry-run` both succeed. `git log --follow` reaches 2025-11-22 through every moved file. The only code file touched is a five-line comment in `server/templateLoader.js` naming `Ship_Info/raw_json`'s new path, re-wrapped to hold the file's line count so the index could not move |
| **the mining bonus in the AI exports (follow-up to `a615018`)** | — | War-room **§7** now carries the observer's mine-output multipliers, each **attributed to the project that grants it** — `Water ×1.15 from Project_ThermalMiningTechniques` on the live save, with the other four reading `×1 (list read, no completed grant)` — plus the multiplicative stacking rule and the statement that every adjusted figure is a **LOWER BOUND** because the mine module's own `miningModifier` is still excluded. The 1.0–4.0 range is **read out of `UNMODELLED_FACTORS`** rather than retyped, so the caveat and the record that declares the factor unmodelled cannot disagree. **Three states, not two**: an unreadable project list renders `UNKNOWN` with the reason, never a measured `×1`; and because `renderWarRoomMarkdown` resolves the observer with `fallbackToFirst: true`, a payload that does not carry the requested observer falls back to a *rival* whose player-mode list is truncated to five entries — that also resolves to UNKNOWN, which is the leak this repo keeps closing. **The block publishes no mining rate of its own**, deliberately: §7's Monthly Net column is the save's own 30-day transaction ledger — realised income with every bonus already inside it — so the block says it must NOT be adjusted again and points the multiplier at `/api/intel/mining` and `/api/intel/mining-expansion`, where the pre-bonus deposit rates actually live. **Byte cost 792 in both modes**: war room **22,527 → 23,321** (player) and **22,494 → 23,288** (omniscient) against 30,720, leaving 7,399 / 7,432 of headroom. `/latest-threats.md` (7,386 / 7,403), `/latest-snapshot.md` (14,835 / 14,987) and all ten sampled intel payloads are **byte-identical**; the diff is the five new lines and nothing else, measured against a frozen `ExitSave.gz` (md5 `5c0d9ef9…`) with two identical baseline runs first. The block sits **inside** §7's body rather than beside it, so the last-resort clamp can suppress it with the rest of the section: 792 bytes of never-degrading fixed overhead would have eaten most of the 409 bytes the 20×-growth case had left. Re-measured at 20× with a fully populated §10 it renders **30,360** (player) / **30,347** (omniscient) against 30,720, no section body suppressed and the mine block intact — the ladder absorbed the growth by shedding more fleet rows. 10 new tests, **8 deliberate mutations each confirmed red** — one of which started GREEN and exposed a test too weak to catch the fallback-observer leak, and was rewritten |
| **the confident 5000 in `runMonteCarloSimulation`** | — | `server/commentary/simulation.js` opened at `let ownRating = 5000; // default baseline`, with `'Combat Hull'` and `'Standard Combatant'` beside it — three invented values for one unmeasured quantity. It now follows the precedent `shared/fleetEngagement.mjs` already set: `available: false`, `ownRating: null`, and a reason that **says no default was substituted**. The guard sits **before** the opponent tiers are built, because player mode scales every opponent rating off `ownRating` and a null there would have swept a set of zeroes. **Two further defects surfaced while checking the consumers.** (1) `server/commentary/index.js` built `reason` on both unavailable branches and then **dropped it** on the way out, so the pre-existing zero-opponent guard was already invisible; the COMMAND panel rendered `available: false` as *nothing at all*, which a reader cannot tell from "the model found nothing to warn about". The panel now renders `COMBAT THRESHOLDS / NOT SIMULATED` with the reason, verified in a real browser at 3117 by driving the renderer with an unavailable result: 0 simulation rows, no `UNWINNABLE`, no hull count. (2) The `"1 hulls"` label was **not** in `simulation.js` as the brief had it — `cea938d` moved it to `shared/engagementModel.mjs`, and `shared/fleetEngagement.mjs` kept a *second copy* of the pluralisation arithmetic with a comment saying it could not fix the first without breaking that change's byte-identity proof. That proof is banked, so both collapse into one exported `hullBandLabel`. **Live on this save**, both modes: the median-alien-escort tier reads `1 hulls → 1 hull`. Every other band, `/latest-threats.md` and `/api/intel/fleet-engagement` are **byte-identical** in both modes — the shared label reproduces the old strings exactly, including the `at least ` floor prefix. 6 new tests, **9 deliberate mutations each confirmed red** |
| **`fleet-engagement-spec.md` (tracker item 4)** | — | **Shipped, and three of the spec's own premises were wrong on re-measurement.** THREAT gains a per-fleet panel and `/api/intel/fleet-engagement`: each alien fleet's opponent rating is composed over **its own ships**, one at a time, not N copies of an archetype. Re-measured 2026-08-22, both modes: 57 alien fleets / 420 ships against 4 observer fleets, sizes `34,26,25,24,23,22,21,20,19,18,16,14,13,13,10,10,9,8,7,7,6,5,4×4,3×7,2×9,1×15` — **26 of 57** past the three-ship tier ceiling and **3 of 57** past the 24-hull sweep, exactly as the spec said. **(1) The observer is 38 hulls, not 4.** The spec's "4 fleets are the constraint" reads as four hulls; India-305 alone is 35. **(2) The `beyond-delta-v` path does not exercise on this save** — the spec asserted it must. The observer's best fleet carries 77.0 km/s against a 28.0 km/s worst modelled destination, so nothing is out of ΔV reach; the real gate is **unknown** (40 of 57), because only 4 of 15 alien bodies are in the shared destination table. It is unit-tested synthetically instead. **(3) "Not winnable" is not a conclusion this model can reach.** `simulateEngagement` is monotone in hull count — every trial is an outright win above `1.65 × ratingRatio`, read off its own arithmetic — so `winnable: false` always meant "above the ceiling I swept". `guaranteedWinHullCount` computes that bound in closed form, the per-fleet sweep sizes its ceiling from it, and past `MAX_ENGAGEMENT_HULLS` (200) the verdict is **beyond-modelled-range** with a floor, never a band pinned at the ceiling. At the old 24-hull ceiling five of the six largest fleets reported "not winnable" where the truth is 28–48. **A fourth measurement changed the player-mode model:** over 420 alien ships, `armorMedian` correlates **−0.077** with design combat value while weapon-system count correlates **+0.798**, and an armour-anchored player rating under-rated one fleet **5.5×** against omniscient (0.183) where the weapon anchor never fell below 0.81×. Player mode is therefore anchored on weapon systems and stays systematically pessimistic (mean 1.83× omniscient), which is the correct direction. Three largest, omniscient → player: Victor-620 (34 ships / 17 hull types) **46–48 → 69–73**, Victor-392 (26/13) **30–32 → 52–55**, Victor-75 (25/17) **28–30 → 53–57**; all three reachability-**unknown**, so all three are labelled and none is withheld — withholding would make an unevaluated threat read as no threat. Reachability reuses `shared/intel/mobility.mjs`'s table by calling it, not copying it, and keeps its tri-state; co-location is the one measured branch. The sweep moved to `shared/engagementModel.mjs` and the PRNG to `shared/prng.mjs` so the worker runs the same model, with `server/commentary/*` left as barrels re-exporting the same function objects — **proven byte-identical across 100 surfaces in both modes**, two identical baseline runs first. `/latest-threats.md` **4,182 → 7,386** (player) and **4,193 → 7,403** (omniscient) against 10,240. Mobile: **0 unreachable** at 375/414/768 in both modes (THREAT raw overflow 127/114/0, all inside the new `overflow-x: auto` wrapper); COMMAND 2.74 / 2.80 screens at 1920. 42 new tests, **24 deliberate mutations each confirmed red** — two of which exposed tests too weak to fail and were rewritten |
| **the four missing AI-export fields (tracker item 1b)** | — | **Re-measured first, and all four were genuinely still absent** from `shared/markdownExports.mjs`: 0 occurrences of `riskFloor`, `benched` or `chain`, and the only 3 hits for `reachab` were §9's *destination* reachability, a different quantity. **The brief's framing was wrong about two of them, and usefully so.** `riskFloor` and `benchedOmittedCount` are `allocateCyclePlan` output — Node CommonJS reading config — so the shared renderer, which also runs in the Cloudflare Worker, cannot compute them. They are now handed IN: the Express route passes `options.cyclePlan`, and the renderer falls back to `snapshot.missionControlBriefing.engineDirectives.cyclePlan`, which every published row already carries, so **the hosted worker needed no change at all**. A runtime with neither says the plan was not read rather than printing a floor of 0 and an empty bench. New **§10 Council Cycle Plan** carries the floor in four distinguished states (unread / not configured / configured-at-0 / in force) and the bench as `8 of 46 · 38 omitted` (player) / `8 of 427 · 419 omitted` (omniscient) **with the ordering rule stated** — the slice is generation order, not the top eight. `chain` and `reachability` land in §8 as a **Research Chain Promotion & Reachability** block: 10 promoted / 1 declined, the declined one being the Pion Torch chain refused at 413.5 months against a 106.9-month measured horizon, with `promotedOmittedCount` carried through from the endpoint's own group limit rather than re-capped here. War room **18,890 → 22,191 bytes** (player) and **18,851 → 22,158** (omniscient) against 30,720, leaving 8,529 / 8,562 of headroom; §8's new block is 1,819 bytes and §10 is 861 / 867. `/latest-threats.md` (4,182 / 4,193) and `/latest-snapshot.md` (14,835 / 14,987) are byte-unchanged. The budget still binds hard: at 20× fleet growth with a fully populated §10 the document renders **30,311** against an unbudgeted **165,711**. 17 new tests, 14 deliberate mutations each confirmed red |
| **war-room §9 cruise acceleration (tracker item 3e)** | — | **Shipped with the third ranking line, and the measurement that decided it points the other way from the brief.** Over the whole 541-drive rated catalogue against the observer's design, best-by-combat and best-by-cruise share **0 of their top 10** (Pion Torch x6 at 606 m/s² burst / 10.1 sustained versus Neutron Liquid Rocket x6 at 20.6 / 20.6) — so it is a genuine second ranking. But inside the population §9 actually ranks — fittable **today**, reactor-compatible, computable — the two winners are the **same drive on 10 of 10** of the observer's designs that have any computable option, in both modes, because everything fittable today caps at thrust cap 1. The line ships anyway: a reader cannot tell "the rankings agree" from "only one ranking was run", and when they agree it renders compactly (~199 bytes) instead of repeating a 178-byte dry-mass caveat. **A second defect was closed in passing**: §9 rendered acceleration with `toFixed(3)`, the confident-zero the DRIVES panel fixed in `7352a44`; `accelOr` now keeps three significant figures, so 0.00016846 reads as `0.000168` rather than `0.000`. Latent, not live — §9's own population bottoms out at 0.018194 on this save. Also `driveExplorerLines` resolved its mode from `intelMode \|\| visibility` and never `mode`, so a local omniscient snapshot rated its drives as player; fixed, and proven byte-identical before/after in both modes. §9 **1,433 → 2,054 bytes**, roughly 620 of which is the cruise figures, the burst-versus-sustained caveat and the `&sort=cruise-acceleration` pointer |
| `mining-tech-bonus-spec.md` | — | **The gating question is answered: the save's per-site rates are PRE-bonus, so the fix was real.** The spec's own proposed measurement (profile richness × `siteDensity` × mine output) is not computable — `TIMiningProfileTemplate.json` stores a random *distribution* per resource (mean/width/min/jump), not a constant, so no template arithmetic reproduces a site's stored rate. Three other measurements settle it instead. **(1)** 280 of the save's 409 hab sites have no mine module and no owner, and all 280 carry non-zero `*_day` rates. **(2)** 405 of 409 sites are byte-identical between `initiative.gz` (2029-09-10) and `ExitSave.gz` (2035-01-01) — 5.3 in-game years in which 90 sites changed owner, 102 changed mine tier, and five factions completed fourteen new mining-bonus projects, the observer going from **zero** mining projects to `Project_ThermalMiningTechniques`. The four sites that moved each moved by one scalar applied to all five resources at once (×1.2497, ×0.5004, ×0.4939, ×1.2412), one of them unowned in both saves, and none is 1.15. **(3)** The income model `Σ(site.rate × mineModule.miningModifier) × 365.25/12 × 1.15^grants` reproduces `cachedYearlyRevenue / 12` at **0.000% error on all five resources for five of eight factions** — every digit the save carries. **Stacking is measured, not assumed: multiplicative.** Humanity First holds both noble-metal projects and reads **1.322500 = 1.15²** against its own unbonused resources, not 1.30 and not a 1.15 cap. `shared/miningTechBonus.mjs` applies it to the three surfaces the spec named — `scoreMiningSiteCandidate` yields, `getMiningRateSummary`, and the owned-hab monthly output feeding **directive Advise economics** — and to nothing else. **Measured before → after, both modes, 36 endpoints:** exactly four payloads moved. Water rose ×1.15 on **81 of 85** scored sites (the other four have no water); volatiles, metals, nobles and fissiles moved on **zero** sites. `siteValue` moved on 23 of 85 and **66 of 85 sites changed rank** (largest shift −19, Kilroy Catena 62 → 43). Briefing water reads 22.7 → **26.1**/day with the grant named in the sentence. In the cycle plan the *committed* set is unchanged in both modes (6 player / 4 omniscient, same councilors and titles); what moved is the valuation and ordering of the Advise-hab alternatives. **`monthlyIncome` is byte-identical** — it is the measured 30-day ledger and the game applied every bonus before writing it. An unresolvable multiplier is `null` and `unknown`, never 1.0, and the raw figure survives beside it labelled a lower bound; the additive `SpaceMiningBonus` fractions are declared unhandled rather than folded in with the multipliers. 22 deliberate mutations, each confirmed red — one of them caught a real leak this change introduced, a doc string naming a rival faction's two completed projects inside a player-mode payload |
| **the total-war gate (tracker item 1)** | `85c8f8a` | **The framing was right about the verdict and wrong about the evidence, in the useful direction.** Both defects were real and both are fixed, but the start year did not need deriving from dated records: the save carries `campaignStartYear` outright, in **`TIGlobalResearchState`** rather than `TIMetadataState` — present in **14 of 14** saves, 2026 on this `2026Start` campaign and 2022 on the older `ModernDayStart` ones (so the 2022 constant was right for those and wrong by four years for this one). `TITimeState.daysInCampaign` is better still, also 14 of 14: it is the game's own campaign-duration counter, so it needs no subtraction. 3256 days puts the campaign start at **2026-02-01**, the same date the earliest-record walk found, and the scenario is literally named `2026Scenario` / `2026Start` — four independent readings, one answer, and no walker built. Year subtraction is kept only as the second fallback because it systematically over-reports: a campaign begun 2026-12-31 and now reading 2036-01-01 scores 10 elapsed years and opens a 10-year gate nine years early. `campaignStartYearMeasured` therefore becomes **`true`**, not false — the flag's target existed all along in a state nobody looked in, and reporting it false would understate provenance. **Measured before → after, both modes:** year gate `20 → 10`, elapsed `13 → 8.91`, remaining `7 → 1.09`, `progressionSpeedAssumed true → false`, `maximumAlienHate 2300 → 2782`. A **third** defect surfaced only once a real speed was passed in: the wiki (Aliens, "Alien Progression Rate", raw wikitext read 2026-08-21 at the wiki's new home `wiki.hoodedhorse.com` — the fandom mirror is now 410) states *"Increase in Alien Maximum Hate per Year is multiplied by X%"* alongside *"Every 'Years Before Aliens Can Do Something' timer has its duration divided by X%"*. Only the threshold half was implemented, so max hate would have shipped at 1891 instead of 2782. **Research reachability moved, deliberately**: the horizon is defined as the campaign already played, so `156 → 106.9` months, `490,558 → 336,158` points, and `horizonAssumed true → false`. No chain changed side of it (`promotedCount` 10, beyond-horizon 11, both modes). No directive verdict flipped. Elapsed time is now resolved once in `shared/campaignElapsed.mjs` instead of twice in parallel |
| `drive-columns-and-filters-spec.md` | `7352a44` | **Part A:** the `CRUISE ACCEL` sort now has a column. Reproduced on the live save: `combat / cruise` equals each drive's own `thrustCap` on **all 541** rows (0 mismatches at 1e-4 relative tolerance), and only **72** have the two equal, so the column is not a restatement. Both acceleration columns now render to three **significant figures**, not three decimals — the previous `toFixed(3)` printed the catalogue's smallest measured acceleration (0.00016846 m/s², shared by 72 drives at `thrustCap` 1) as `0.000`, a confident-looking zero that had been on screen in the COMBAT column since `5f4cd64`. **Part B:** `minDeltaV` / `minCombatAcceleration` / `minCruiseAcceleration` on `/api/intel/drive-explorer`, parsed once in `shared/requestValidation.mjs` so the Express route, the Cloudflare worker and the panel cannot drift; the browser applies the same rule against rows it already holds rather than re-fetching, and a parity test compares both over a 10-threshold × 3-population matrix in both modes. **Untestable is not failure**, in Kleene three-valued logic: a definite failure on any testable minimum is a failure, but a row whose filtered field is unmeasured is excluded and counted apart, named with the measurement that was missing. Live figures: `minDeltaV=10` → 301 matched / 240 below / 0 untestable; `+minCombatAcceleration=20` → 105; against a design with no hull in service, 0 matched / **0 below / 541 untestable**, which `Number(null) === 0` would have reported as 541 measured failures. 14 deliberate mutations, each confirmed red. Mobile: **0** unreachable at 375/414/768 in both modes, unchanged by the eighth column and three new controls; COMMAND 2.74 / 2.80 screens at 1920. War-room §9 deliberately deferred — see open item 3e |
| `drive-explorer-spec.md` | `5f4cd64` | DRIVES view + `/api/intel/drive-explorer` + war-room §9. All 541 drives against one design, none hidden: 37 fittable / 486 researchable / 18 never / 0 unresolved on the live save, and 306 of 541 marked reactor-incompatible with the class they would need. The measured half (ΔV, acceleration) and the estimated half (destination reach) render in two different registers, asserted by computed style in `scripts/verify_drive_explorer.js`. `?limit=` needed a scoped 1,000-row ceiling — `CATALOGUE_LIMIT_BOUNDS`, shared so both runtimes decide alike |
| `mobile-and-tech-search-spec.md` | `dda9b25` | **Part A's premise was wrong and the measurement says so.** `mc-board-table` was never broken: its wrapper measured `canScroll=true, maxScrollLeft=467, fullyRevealed=true`, so expansion's 164 / threat's 147 / records' 45 were reachable by scrolling, not clipped. The only genuinely unreachable content was DRIVES — 64 elements with no scrollable ancestor, worst edge 937px in a 375px viewport. One line caused it: the `max-width: 900px` block turns `.init-view__grid` into a column flex container and the base rule's `align-items: start` carried over, so `.init-view__span` shrink-wrapped to the table's 937px max-content and `.de-table-wrap` measured `clientWidth 890 / scrollWidth 890` — its `overflow-x: auto` had nothing to scroll. Stretching the items fixed it (`313/890`). Generalising FLEET's `min-width: 100%; table-layout: fixed` to narrow widths then took command/expansion/threat to **zero** overflow rather than merely scrollable. Now 0 unreachable at 375/414/768 in both modes; COMMAND 2.74 / 2.80 screens at 1920. Part B is a RECORDS panel over the existing `tech-search` + `tech-tree` rows: 165 unlocked of 750, `Copperhead` → *Hydrolox High Explosive Missiles*. `scripts/verify_mobile_overflow.js` measures it off a live DOM |
| three review findings on `dda9b25` | `1b1b7ec` | An independent review flagged the third scroll hint as revealed by width alone. **The measurement inverted the finding**: below 900px those tables genuinely overflow (`min-width: 840px` vs a 632px wrapper), so the un-gated reveal was accidentally truthful — true by construction, false the moment either number moves. The real defect was above the breakpoint, hint hidden while the table still overflowed (905px: 637 vs 840; 1040px: 772 vs 840). A guard now derives the styled-hint set from the stylesheet and fails on any unregistered one; there is no fourth today. Also `unlocked-tech` printing "0 unlocked of 0 projects" when the graph is unreadable — the body carried the same false claim independently of the footer, so both are gated on the census |
| **possible confident zero in the §9 refit caveat** | — | **CLOSED 2026-08-22: the zero is genuine, and the actual defect was the state nobody asked about.** The caveat reads "this drive's fixed mass is 78 t against the fitted drive's **0 t**", and 0 t there is either a reading or a null coerced to one. **Measured against `TIDriveTemplate.json`: all 541 drives carry `flatMass_tons` explicitly, 487 at a genuine 0 and 54 non-zero** — and `stat()` in `server/snapshot/templates.js` already maps an absent field to null rather than 0, so the coercion was never possible from the shipped templates. The surface now says so: a caveat naming a zero adds "*(both read from the drive templates; a 0 t here is a measurement, not an absent field)*", and only when one side is zero, so the sentence is unchanged everywhere else. **The real bug was the third state.** `describeDryMassChange` replaced `fitted !== null && candidate !== null && fitted !== candidate ? caveat : null`, under which an UNREADABLE fixed mass on either side produced no caveat at all — and no caveat reads as "dry mass is unaffected", a check that could not be evaluated falling through to the reassuring answer. It now reports UNKNOWN in all three directions. Latent on the shipped templates, reachable from a snapshot published before `flatMass_tons` was baked. Measured on the live save, both modes: 54 of 541 rows raise the caveat, all 54 naming a measured 0 t on the fitted side (Burner Drive x6), 0 unknown. Three deliberate mutations — reverting to the old expression, dropping the disambiguation, firing on equal masses — each confirmed red |
| `.gitattributes` for generated files | `795c5fe` | `docs/code-index.md` and `tests/fixtures/frozen-snapshot-*.md` pinned to LF, so a fresh clone or agent worktree no longer fails two byte-comparison tests for an environment reason |
| `drive-path-modal-spec.md` | `065ec73` | Clicking a drive row opened nothing — the rows carried no handler, no `role`, no `tabindex` and no cursor. Now every row's name is a real `<button>` (Enter/Space native, `aria-label` naming what it does) that opens the shared `MissionControlDetailPanel`, extended with grouped `sections` and caveat `notes` rather than duplicated. **The backend gap was the satisfied half**: `buildTechPath` collects only the remaining path, so `alreadyCompleted` returned 0 even on a 12-step path. Added `satisfiedPrerequisites` / `…TotalCount` / `…OmittedCount` on `/api/intel/tech-path`, following the CHOSEN branch only — the Pion Torch gate reports **11 satisfied** against 12 remaining (7 faction / 5 global, 995,000 / 305,325). **A second defect was found while building it**: `remainingPath` is a PRE-order walk, so reversing it is not a dependency order — `Project_ExoticHybridSystems` and `Project_Exotics` are siblings under one parent while the first needs the second, and the reversed order listed the dependent first. `remainingPathDependencyOrder` (post-order, first-occurrence dedupe) is the real topological order; a property test asserts every prerequisite precedes its dependent. `remainingPath`, every cost field and `routesEvaluated` are **byte-identical** before and after across 899 nodes × 2 modes, with two identical baseline runs proving the harness deterministic. `availabilityCaveat` rides the payload so an agent sees the rolled-availability caveat too (§3b), and `researchCost: -1` renders as `NEVER RESEARCHED` with the total as `UNKNOWN`, never a smaller number. `scripts/verify_drive_path_modal.js` drives a real click and a real `Enter`, and re-measures the open modal at 375/414/768. **No war-room change**: this is a per-drive drill-down, not a briefing fact, and §9 already links `/api/intel/drive-explorer`, whose `detail=full` chain record carries `pathEndpoint` to the path itself |
| `code-index-spec.md` | `bbef9f0` | generated index, required agent reading |
| `research-vs-procurement-spec.md` | `baaa38a` | also repaired the self-referential `--text-dim` token that silently broke 164 rules |
| `save-autodetect-spec.md` | `b0ec6dc` | `/api/save-state`, 5 s visibility-gated poller, opt-in auto-load, 503 retry |
| `research-tab-layout-spec.md` | `b0ec6dc` | eight font sizes → four; 41% empty column fixed; per-row global badge removed |
| `fleet-procurement-spec.md` | `36fa5ba` `2b6e3d5` | FLEET view; refit advisor with non-composability enforced; armour gap indicator |
| `obsolete-marker-spec.md` | `cdceae7` | the parts filter was a real correctness fix — it was recommending a retired weapon |
| `research-chain-spec.md` | `e98413f` | alternate-prereq semantics, `routesEvaluated`, whole-chain drive payoffs, first-in-class verdicts |
| `risk-tolerance-spec.md` | `b3b77f6` | success floor as a pairing-scoped registry veto, testing the band low not the midpoint |
| `research-row-naming-spec.md` | — | was already implemented; this table previously said otherwise |
| `model-verification-review.md` | `b0ec6dc` `e98413f` | all findings actioned |
| `research-advisor-spec.md` | through §9 | still the governing document: §0 (nothing campaign-specific) and §3b (availability is rolled, not derived) bind any further work |
| `chain-visibility-spec.md` | `bdcff55` | reachable chains promoted into COMMAND; `Colony Core → Battlestations` now on screen. Pion Torch refused at 413 months against a 156-month horizon |
| `campaign-settings-spec.md` | `39770d0` | ten settings baked; a custom campaign no longer reads "Normal". Proven not to move any figure across 152 surfaces × 3 modes |
| type scale follow-up | — | 11/10/9.5/9 → 12.5/11/10/9; every step now ≥1px apart. COMMAND at 1920 measured 2.858 (player) / 2.915 (omniscient) screens, 0 leaf overflows |
| **`value/gdp-per-cp-cost`, and the weight it was tuned against** (the defect filed by `control-point-cap-spec.md`) | — | **The bug was a SHAPE error, not a scale one, so "keep the old ranking" was never on the table.** The rule divided a nation's output by its control-point count while charging one control point the **whole nation's** cost; the wiki (`Nations` § "Cost of Control Points", raw wikitext, 2026-08-22) divides both sides, so the count cancels and the density is `2 × GDP_Bn^0.4`. Measured on `ExitSave.gz`: the omission put **921 of 11,781 nation pairs (7.8%) in the wrong order** — Russia ranked above the larger United Malay Nation, Belgium-Luxembourg above Pakistan and Egypt, Kazakhstan and Australia above Nigeria. No scalar weight can un-invert those, so the ordering had to move. `WEIGHTS.VALUE_POINTS` **1 → 0.5** (and `config/defaults.json`, where the running server actually reads it) holds the family on the anchor the other four families are documented against — a small-nation grab at 4.4–5.5 — and 0.5 is the factor that leaves the **modal** nation exactly fixed (81 of 154 have two control points). Swept 1 / 0.5 / 0.2 in both modes: at 1.0 the allocator stacks two councilors on China and drops Defend Interests; at 0.2 it loses an Advise continuation; 0.5 keeps every assignment and promotes the corrected top target. **Omniscient**: primary flips "Advise Government: USA" (6.997) → "Purge the Protectorate hold on ExtractiveSector in China" (20.92 → **68.75**), `totalExpectedValue` 21.41 → 66.13, same five councilors on the same five actions. **Player**: cycle plan byte-identical; the 368-strong `uncertain` board re-ranks, with the United Malay Nation now correctly above Russia. Whole-briefing diff confined to `engineDirectives`. **Gap for the markdownExports owner**: none of this reaches `/latest-war-room.md` — §10 prints counts only, and those did not change, so the primary recommendation, its score and `totalExpectedValue` are invisible to every LLM reading the exports |
| `benched` truncation follow-up | — | `benchedTotalCount` / `benchedOmittedCount` on the cycle plan and through the board. Live save: 8 of 46 shown (player), 8 of 427 (omniscient). Also fixed `counts.assigned` / `counts.benched`, which were read off a `counts` object that never carried them and rendered a confident "0 allocated · 0 benched" on every plan |

## Closed as needing no work

Recorded because the measurement is the deliverable, and because acting on the assumption
would have introduced errors.

- **THREAT half-track table cards (post-split follow-up).** Measured 2026-08-24 at
  1440 / 1600 / 1920 in both modes: four board tables in side-by-side cards need
  728–811px in 652–732px bodies; scroll hints are truthful. Spanning to full width
  trades ~50px of announced horizontal scroll for roughly doubling THREAT height — left
  as-is per `docs/post-split-cleanup-spec.md`.
- **`.mc-skill-*` in `16-board-skill-cells.css` and `.faction-intel-*` on two surfaces.**
  Cosmetic naming/placement debts from the split. A merge or rename is behaviour-visible
  and cascade-order-sensitive for no measured user gain; left as-is.
- **Campaign rate multipliers.** Research, mining, national IP and alien progression all
  run at 200% and are **already reflected**, because the dashboard reads measured values
  rather than computing from base rates. Applying a 2× correction would have broken correct
  figures. Evidence in `campaign-settings-spec.md`.
- **Engineers (+95%).** Already inside measured research income. Applying separately would
  double-count.
- **Pricing durations through the allocation model** (was open item 3b, closed 2026-08-22
  in `d3ce7e4`). The contradiction between `research-category-rate-spec.md` and
  `campaign-settings-spec.md` is settled, and **no duration moved**. Both measurements were
  real; the *inference* from one was a units error. The 2.11× is the observer's four slots
  delivering `cachedYearlyRevenue.Research` **summed**, while a duration concerns one slot —
  and the four slots measured 0.4658×, 0.2928×, 1.0602× and 0.2928×, which sum to that
  2.11×. Three are **below 1**, so the flat figure is already 2.15×–3.42× *optimistic*
  there rather than conservative; dividing by 2.11 would have taken the 1-pip slots to 7.2×
  optimistic, in the wrong direction. `campaign-settings-spec.md` reached the right verdict
  by invalid reasoning — a delivered/predicted ratio near 1.0 cannot detect a missing ×2
  when the prediction carries a fitted `ProjectBonus` of −0.209 — and its recorded pip
  layout did not match the saves it named. Replacement evidence: measured gain **2.1115×**
  against **2.1420×** predicted from terms all read from the save, where a pre-200% income
  would have needed 4.2840×. A project pip delivers **1.885714×** a global-tech pip against
  `1.98 / 1.05 = 1.885714` predicted, to six figures — a campaign-wide multiplier cannot
  produce a ratio *between slot kinds*, which is what makes this decisive.
  **Recorded, not built:** the fastest-achievable duration (all pips on one slot) is a real
  lower bound at `1.05 × (1 + Category + Project)`, but it is category-dependent (1.1025×
  to 2.5095×) and needs a counterfactual allocation per candidate.

## Archive

`archive/` holds finished plans, shipped specs and completed reviews. They are kept because
**source comments cite them** — they carry the reasoning behind decisions in the code, and
several record a model that was tried and rejected, which is worth as much as the one that
shipped.

Notable: `archive/directive-rule-engine-plan.md` is the v1 engine, superseded by
`archive/directive-engine-v2*.md`. `archive/strategic-intelligence-suite/` holds earlier
reviews and still cites four dead browser scripts by their repository-root paths.
`archive/repo-structure-spec.md` records the 2026-08-21 measurement of the root; three of
its figures were wrong and the Shipped row above says which.

## Conventions

Specs here record **what was measured**, not what was assumed. Where a model was rejected,
the rejection and its evidence stay in the document rather than being deleted —
`research-advisor-spec.md` keeps a superseded armour derivation for exactly that reason,
and `campaign-settings-spec.md` keeps two discarded measurement attempts so they are not
repeated.

When archiving, update the citing source comments in the same commit; the paths are
load-bearing.
