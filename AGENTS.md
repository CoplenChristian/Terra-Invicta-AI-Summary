# See CLAUDE.md

This file's contents were merged into [`CLAUDE.md`](./CLAUDE.md) on 2026-08-20, which is now the single canonical set of instructions for agents working in this repository.

**Do not add content here.** Anything added to this file will drift out of sync with `CLAUDE.md` and agents will act on whichever they happen to read first.

> A real symlink was intended, but creating one on Windows requires elevation or Developer Mode. If you enable either, replace this file with:
>
> ```bash
> rm AGENTS.md && ln -s CLAUDE.md AGENTS.md
> ```
